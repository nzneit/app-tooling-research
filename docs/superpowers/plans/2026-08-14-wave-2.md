# Wave 2 (0060 + 0070) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Execute Wave 2 of the App Tooling Research program: two validator maintenance fixes, then research plans, surveys, and drafted reports for tracks 0060-transport-abstraction and 0070-state-concurrency, both grounded in 0010's accepted report.

**Architecture:** Same per-track lifecycle as Wave 1 (research-plan → survey → report with STE summary → user acceptance gate), with the Wave 1 candidate-gap sweep folded into each survey workflow as a standing gap-scan agent instead of a separate pass. Tasks 1–3 are file edits executed via subagent-driven development; Tasks 4–5 are controller-run survey workflows (investigators + synthesis + completeness critic per the spec's execution model); Task 6 is a cross-track coherence pass ending at the user acceptance gate.

**Tech Stack:** Markdown docs; Node 24 native TS (`node scripts/check-docs.ts`, `node --test`); Workflow tool for surveys; no new dependencies.

**Spec:** `docs/superpowers/specs/2026-08-13-tooling-research-program-design.md` (sections "Wave 2 — architecture tracks", "Shared evaluation rubric", "Execution model", "Report summaries"). Wave 2 handoffs come from `tracks/0010-contract-pipeline/report.md` (accepted, D-0010) and `docs/2026-08-13-wave-1-close-out.md`.

## Global Constraints

- **D-0001** survey-only: no prototyping; open verifications land in each report's "What a spike would validate" section.
- **D-0002** pragmatic lint mix: oxlint stays; ESLint or standalone CLIs allowed in CI only.
- **D-0003 + D-0011** OSS only: no paid or self-hosted-server products; eslint-plugin-sonarjs releases after v2.0.4 (SSALv1) are excluded.
- **D-0004** grounding: `facts/app-profile.md` is still unfilled — every report claim cites a fact or declares its assumption in place.
- **D-0006** live validation of all contracted inbound messages; 0060 is the choke point — no code path may consume a contracted message around it.
- **D-0007** STE summary prepended to every report, first H2 exactly `## Summary (STE)`, max 2 paragraphs, order: examined → recommendation → risk → next step.
- Track statuses come from the fixed vocabulary: `planned → surveying → report drafted → accepted` (+ `deferred`). README row and frontier updates ride the same commit as the track change they describe.
- Run `node scripts/check-docs.ts` before every commit; it must exit 0.
- **No AI attribution in commits** (user instruction, in memory): no Co-Authored-By or "Generated with" trailers, in this session's commits and in any subagent brief that commits.
- Commits stay local; the user owns pushes to `github.com/nzneit/app-tooling-research`.

---

### Task 1: Validator maintenance (two close-out items)

The Wave 1 close-out (`docs/2026-08-13-wave-1-close-out.md`, "Before Wave 2 starts") flags two fixes in `scripts/check-docs.ts` before spike-era files appear.

**Files:**
- Modify: `scripts/check-docs.ts` (main() at lines ~232–238; new exported helper near `checkReports`)
- Test: `scripts/check-docs.test.ts`
- Modify: `docs/2026-08-13-wave-1-close-out.md` (mark both items done)

**Interfaces:**
- Produces: `isTrackReport(path: string): boolean` exported from `scripts/check-docs.ts` — true only for `tracks/<dir>/report.md`, so future `tracks/*/spikes/*/report.md` and `draft-report.md` files are not policed for an STE summary.

- [ ] **Step 1: Write the failing test**

Append to `scripts/check-docs.test.ts` (add `isTrackReport` to the existing import from `./check-docs.ts`):

```ts
test("isTrackReport matches only tracks/<dir>/report.md", () => {
  assert.ok(isTrackReport("tracks/0060-transport-abstraction/report.md"));
  assert.ok(!isTrackReport("tracks/0010-contract-pipeline/spikes/orval-wrap/report.md"));
  assert.ok(!isTrackReport("tracks/0010-contract-pipeline/draft-report.md"));
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test scripts/check-docs.test.ts`
Expected: FAIL — `isTrackReport` is not exported.

- [ ] **Step 3: Implement**

In `scripts/check-docs.ts`, above `checkReports`:

```ts
// Only a track's own top-level report is policed for the STE summary —
// not spike reports or drafts (close-out item 1, 2026-08-13).
export function isTrackReport(path: string): boolean {
  return /^tracks\/[^/]+\/report\.md$/.test(path);
}
```

In `main()`, replace the reports line:

```ts
const reports = listMarkdown("tracks").filter((f) => isTrackReport(f.path));
```

And add intake files to the link-checked set (close-out item 2) inside the `linkedDocs` array:

```ts
...listMarkdown("intake"),
```

- [ ] **Step 4: Run tests and the validator**

Run: `node --test scripts/check-docs.test.ts` — expected: all pass (19 tests).
Run: `node scripts/check-docs.ts` — expected: `check-docs: ok` with the same counts as before (intake files currently contain no markdown links, verified 2026-08-14, so no new errors appear).

- [ ] **Step 5: Mark the close-out items done**

In `docs/2026-08-13-wave-1-close-out.md`, append ` — done 2026-08-14` to numbered items 1 and 2 under "Before Wave 2 starts".

- [ ] **Step 6: Commit**

```bash
git add scripts/check-docs.ts scripts/check-docs.test.ts docs/2026-08-13-wave-1-close-out.md
git commit -m "chore(validator): narrow report path match, link-check intake files"
```

---

### Task 2: 0060-transport-abstraction research plan

**Files:**
- Create: `tracks/0060-transport-abstraction/research-plan.md`
- Modify: `README.md` (0060 index row status `planned` → `surveying`; frontier line)

**Interfaces:**
- Consumes: `templates/research-plan.md` structure; spec section "0060-transport-abstraction"; 0010 report handoffs (choke point, four-class taxonomy, quarantine, coverage layers, per-topic policy).
- Produces: the research plan Task 4's survey executes; key-question numbering 1–8 referenced by the 0060 report.

- [ ] **Step 1: Write the research plan**

Create `tracks/0060-transport-abstraction/research-plan.md` with exactly this content:

```markdown
# 0060-transport-abstraction — research plan

**Status**: draft

## Goal

Decide the shape of a unified, typed transport boundary so UI code never touches mqtt.js
or fetch directly. The boundary is the validation choke point required by D-0006 and
handed off by 0010 (D-0010): it consumes 0010's pipeline artifacts (orval-wrapped REST
client with per-status zod validation; compiled Ajv validators for AsyncAPI payloads),
maps HTTP reason-code bodies into a typed error taxonomy, normalizes both legs' error
shapes into 0010's four-class taxonomy for 0050, and exposes subscriptions and requests
in one idiom. The report decides build vs adopt + wrap composition.

## Key questions

1. Interface shape: event emitter, observable, async iterable, or store-adapter — which
   composes best with the Zustand and xstate consumers (coordinate with 0070)?
2. Where exactly does runtime validation hook in — one ingress per protocol — and how do
   0010's coverage layers (branded types, oxlint no-restricted-imports bans,
   validating-accessor-only exports) attach to this boundary so no code path consumes a
   contracted message around it (D-0006)?
3. Error taxonomy: how do ZodError (REST leg) and Ajv error objects (MQTT leg) normalize
   into 0010's four-class taxonomy (transport error / contract violation / contracted
   business error / unknown topic-or-endpoint), which module owns the taxonomy, and how
   does it surface through 0050's facade as a first-class contract-break signal?
4. Failure plumbing: where do 0010's reject-and-quarantine semantics live (bounded ring
   of raw payload + structured error + topic/endpoint + timestamp), and how is the
   quarantine inspected in dev and in prod?
5. Reconnection, backpressure, and QoS: what does the boundary own
   (resubscribe-on-reconnect, inflight replay, queue bounds, ordering) vs delegate to
   mqtt.js?
6. REST lifecycle: does TanStack Query live inside the boundary (caching, retries,
   dedup, cancellation) or outside it — and where is the seam with 0070's
   race-prevention concerns?
7. Build vs adopt: is this mostly a build with adopted pieces? Which pieces — 0010's
   orval-generated client vs openapi-fetch or ts-rest as the REST surface; RxJS vs
   native async iterables/EventTarget vs xstate actors as the MQTT surface?
8. Per-topic validation policy: where is 0010's per-topic policy table (validate-always
   vs sampled) declared, and how does the boundary enforce it?

## Candidates

- custom boundary layer (build) — the build option, composing 0010's artifacts
- RxJS — https://github.com/ReactiveX/rxjs — observables; retry/backpressure/cancellation operators
- Effect — https://github.com/Effect-TS/effect — typed effects, errors as values, streams
- TanStack Query — https://github.com/TanStack/query — REST request lifecycle (cache, dedup, retries)
- openapi-fetch — https://github.com/openapi-ts/openapi-typescript — ~6 kB typed fetch client over openapi-typescript types
- ts-rest — https://github.com/ts-rest/ts-rest — contract-first REST client with runtime validation
- zodios — https://github.com/ecyrbe/zodios — zod-typed REST client (0010 flags the stack dormant since 2023)
- xstate actors as transport adapters — https://github.com/statelyai/xstate — fromCallback/fromEventObservable wrapping mqtt.js
- mitt — https://github.com/developit/mitt — ~200 B emitter primitive for the build option
- emittery — https://github.com/sindresorhus/emittery — typed async emitter primitive for the build option

## Survey verification notes

- zodios dormancy (0010: @zodios/core last shipped 2023-08-22, Zod ^3 only) — confirm, likely eliminate early.
- TanStack Query scope here is boundary composition (REST lifecycle inside the choke point), not general data-fetching DX.
- The boundary ships to the browser: every candidate scores Browser compatibility and Runtime overhead.

## Rubric weights

| Criterion | Weight |
|---|---|
| License | high |
| Maintenance health | high |
| TypeScript fit | high |
| Browser compatibility | high |
| Contract-format support | medium |
| Integration cost | high |
| Runtime overhead | high |
| Output quality | medium |
| Escape hatch | high |

## Facts needed

- MQTT: topic scheme, QoS levels, peak message rate, reconnect frequency (questions 4–5, 8)
- Whether messages carry ordering metadata (timestamps or sequence numbers)
- REST: approximate operation count; the app's current fetch/client layer
- Browser targets; bundle budget (RxJS and Effect are meaningful adds)
- Team conventions or vetoes on RxJS or Effect adoption
```

- [ ] **Step 2: Flip the README row and frontier**

In `README.md`: change the 0060 row's status cell from `planned` to `surveying`; replace the frontier line with:

```markdown
> **Frontier:** Wave 2 surveying — 0060 research plan drafted; next: 0070 research plan, then both surveys.
```

- [ ] **Step 3: Validate**

Run: `node scripts/check-docs.ts` — expected: `check-docs: ok` with `6 track dir(s)`.

- [ ] **Step 4: Commit**

```bash
git add tracks/0060-transport-abstraction/research-plan.md README.md
git commit -m "feat(0060): draft transport-abstraction research plan"
```

---

### Task 3: 0070-state-concurrency research plan

**Files:**
- Create: `tracks/0070-state-concurrency/research-plan.md`
- Modify: `README.md` (0070 row `planned` → `surveying`; frontier line)

**Interfaces:**
- Consumes: `templates/research-plan.md` structure; spec section "0070-state-concurrency"; 0010/0060 seam (validated ingress, TanStack Query overlap).
- Produces: the research plan Task 5's survey executes; key-question numbering 1–7 referenced by the 0070 report.

- [ ] **Step 1: Write the research plan**

Create `tracks/0070-state-concurrency/research-plan.md` with exactly this content:

```markdown
# 0070-state-concurrency — research plan

**Status**: draft

## Goal

Decide the blessed composition patterns for the Zustand + xstate layer with
race-condition prevention as a first-class design goal. The track produces a race
taxonomy for this app's traffic shape (concurrent MQTT events and REST responses feeding
shared stores), sorts each race into architecturally-solved vs tooling-solved, and
recommends the store-and-machine composition idiom plus the test and lint tooling that
keeps races out. Coordinates with 0060: validated messages enter state only through the
transport boundary.

## Key questions

1. Race taxonomy: which races does this app face — stale REST response vs fresher MQTT
   event, out-of-order MQTT messages, double-submit, optimistic-update rollback,
   reconnect replay — and what evidence exists for each?
2. Which races are solved architecturally — state machines, single-writer stores, actor
   ownership, monotonic guards at ingress — and what are the blessed Zustand-xstate
   composition patterns: which state lives in stores vs machines, and how do actors and
   stores wire together?
3. Which races are solved by tooling — lint rules, fast-check property tests,
   @xstate/test model-based tests — and what is the CI shape?
4. @xstate/store: does it replace or complement Zustand in this stack?
5. TanStack Query overlap with 0060: if the boundary adopts it for REST lifecycle, which
   races disappear for free (dedup, cancellation, staleness tracking) and which remain?
6. Cancellation idiom: AbortController propagation, RxJS switchMap semantics, Effect
   interruption, xstate actor stop — which does the app standardize on, and how does it
   cross the 0060 boundary?
7. Ingress ordering: how does validated 0060 output enter stores — a single dispatch
   path per topic? What guard decides stale-vs-fresh when a REST response races an MQTT
   event?

## Candidates

- xstate v5 actors — https://github.com/statelyai/xstate — actor model, state machines
- @xstate/store — https://github.com/statelyai/xstate — small store from the xstate team
- Zustand middleware patterns — https://github.com/pmndrs/zustand — official middlewares and store patterns
- TanStack Query — https://github.com/TanStack/query — request lifecycle (shared candidate with 0060)
- RxJS cancellation semantics — https://github.com/ReactiveX/rxjs — switchMap/exhaustMap idioms as prior art
- Effect — https://github.com/Effect-TS/effect — structured concurrency, interruption
- fast-check — https://github.com/dubzzz/fast-check — property-based testing; fc.scheduler for race scheduling
- @xstate/test — https://github.com/statelyai/xstate — model-based test generation

## Survey verification notes

- The deliverable is patterns + tooling, not one library: expect a recommendation naming
  an idiom set plus adopted test tooling — possibly "build (patterns) + adopt (test tools)".
- Verify @xstate/test's xstate-v5 status; it historically lagged xstate major versions.
- fast-check's `fc.scheduler()` race-detection arbitrary is the feature to assess, not
  general property testing.

## Rubric weights

| Criterion | Weight |
|---|---|
| License | high |
| Maintenance health | high |
| TypeScript fit | high |
| Browser compatibility | high |
| Contract-format support | n-a |
| Integration cost | high |
| Runtime overhead | medium |
| Output quality | medium |
| Escape hatch | high |

## Facts needed

- Which stores and machines exist today; where races have actually bitten (bug history)
- MQTT ordering metadata availability (shared with 0060)
- Test framework in use (for fast-check and @xstate/test integration)
- Team appetite for RxJS or Effect (shared with 0060)
```

- [ ] **Step 2: Flip the README row and frontier**

In `README.md`: change the 0070 row's status cell from `planned` to `surveying`; replace the frontier line with:

```markdown
> **Frontier:** Wave 2 surveying (0060, 0070) — next: run both surveys and draft reports.
```

- [ ] **Step 3: Validate**

Run: `node scripts/check-docs.ts` — expected: `check-docs: ok` with `7 track dir(s)`.

- [ ] **Step 4: Commit**

```bash
git add tracks/0070-state-concurrency/research-plan.md README.md
git commit -m "feat(0070): draft state-concurrency research plan"
```

---

### Task 4: 0060 survey and report draft (controller-run workflow)

**Files:**
- Create: `tracks/0060-transport-abstraction/report.md`
- Modify: `README.md` (0060 row → `report drafted`; frontier)

**Interfaces:**
- Consumes: Task 2's research plan (questions 1–8, candidates, weights); `templates/report.md` (Summary-first structure); 0010 report sections "Key questions 5–7" and "Risks" (the handoffs).
- Produces: `tracks/0060-transport-abstraction/report.md` whose "Recommendation" section Task 6's coherence pass reads; the error-taxonomy ownership statement 0070's report must agree with.

- [ ] **Step 1: Run the survey workflow**

Controller runs a Workflow (≤15 agents), mirroring Wave 1's shape with the gap sweep folded in:
- One investigator per candidate or candidate pair (batch mitt+emittery; batch zodios+ts-rest), each answering the rubric with source-linked evidence, versions, and access dates.
- One prior-art investigator for the build option: transport-boundary/choke-point structures in shipping OSS (zodios endpoint design, Effect platform HttpClient, xstate `fromEventObservable` docs, TanStack Query's queryFn seam), plus MQTT-over-WSS reconnect/backpressure prior art.
- One gap-scan agent (the Wave 1 sweep, now standing): candidates the plan missed or omitted; challengers to the emerging recommendation.
- One synthesis agent producing the cross-candidate comparison keyed to questions 1–8.
- One completeness critic: unanswered key questions, unverified claims, missing assumption declarations (D-0004).

- [ ] **Step 2: Draft the report**

Write `tracks/0060-transport-abstraction/report.md` following `templates/report.md` and the Wave 1 report structure: `## Summary (STE)` first (D-0007: 2 paragraphs max — examined → recommendation → risk → next step), then **As of** + **Recommendation** lines, `## Survey` (per-candidate, source-linked), `## Key questions` (all 8 answered or explicitly deferred with a reason), `## Rubric comparison` (strong/adequate/weak + evidence, weights from the plan), `## Recommendation` (shape + rationale + risks + assumptions declared per D-0004), `## What a spike would validate`, `## Sources` (every URL with access date). The error-taxonomy answer (question 3) must name the owning module explicitly — Task 6 checks 0070 agrees.

- [ ] **Step 3: Flip status, validate, commit**

README 0060 row → `report drafted`; frontier:

```markdown
> **Frontier:** 0060 report drafted — next: 0070 survey and report.
```

Run: `node scripts/check-docs.ts` — expected: ok.

```bash
git add tracks/0060-transport-abstraction/report.md README.md
git commit -m "feat(0060): draft transport-abstraction survey report"
```

---

### Task 5: 0070 survey and report draft (controller-run workflow)

**Files:**
- Create: `tracks/0070-state-concurrency/report.md`
- Modify: `README.md` (0070 row → `report drafted`; frontier)

**Interfaces:**
- Consumes: Task 3's research plan (questions 1–7); Task 4's drafted 0060 report (the TanStack Query seam answer and taxonomy owner).
- Produces: `tracks/0070-state-concurrency/report.md` for Task 6's coherence pass.

- [ ] **Step 1: Run the survey workflow**

Same shape as Task 4 (≤15 agents): per-candidate investigators (batch RxJS-cancellation + Effect as the "heavyweight idiom" pair; batch fast-check + @xstate/test as the test-tooling pair), one race-taxonomy prior-art investigator (documented React/MQTT race patterns, TanStack Query race handling, xstate v5 actor ownership guidance, Zustand transient-update and single-writer patterns), one gap-scan agent, one synthesis agent keyed to questions 1–7, one completeness critic. The Task 4 report's seam answers are injected into the synthesis prompt so 0070's answers compose with 0060's rather than contradict them.

- [ ] **Step 2: Draft the report**

Write `tracks/0070-state-concurrency/report.md` with the same required structure as Task 4 Step 2 (Summary (STE) first, As of + Recommendation lines, Survey, Key questions all answered or deferred with reasons, Rubric comparison, Recommendation with risks and D-0004 assumption declarations, What a spike would validate, Sources with access dates). Question 5's answer must state the same TanStack Query ownership boundary the 0060 report states.

- [ ] **Step 3: Flip status, validate, commit**

README 0070 row → `report drafted`; frontier:

```markdown
> **Frontier:** Wave 2 reports drafted (0060, 0070) — next: cross-track coherence pass, then user review.
```

Run: `node scripts/check-docs.ts` — expected: ok.

```bash
git add tracks/0070-state-concurrency/report.md README.md
git commit -m "feat(0070): draft state-concurrency survey report"
```

---

### Task 6: Coherence pass and acceptance gate

**Files:**
- Modify (if inconsistencies found): `tracks/0060-transport-abstraction/report.md`, `tracks/0070-state-concurrency/report.md`
- Modify (on user acceptance only): `DECISIONS.md` (D-0015, D-0016), `README.md` (rows → `accepted`; frontier)

**Interfaces:**
- Consumes: both drafted reports; DECISIONS.md (next free IDs are D-0015 and D-0016).
- Produces: the Wave 2 acceptance ledger entries and the closing frontier.

- [ ] **Step 1: Cross-track coherence review**

Dispatch one reviewer subagent over both reports checking exactly: (a) both name the same TanStack Query ownership boundary; (b) exactly one report claims ownership of the four-class error-taxonomy module and the other cites it; (c) the interface-shape recommendation in 0060 is the idiom 0070's composition patterns consume; (d) no candidate is scored contradictorily across the two reports (same version, same license, compatible maintenance verdicts); (e) both reports declare their D-0004 assumptions. Fix findings in place; re-run `node scripts/check-docs.ts`; commit any fixes:

```bash
git add tracks/0060-transport-abstraction/report.md tracks/0070-state-concurrency/report.md
git commit -m "fix(wave-2): align 0060/0070 seam statements"
```

- [ ] **Step 2: Present for acceptance (user gate — stop here)**

Report to the user: both recommendations with their STE summaries, risks, and the seam decisions. Acceptance is the user's call (spec: "User review is the real gate").

- [ ] **Step 3: On acceptance — ledger and status (only after the user accepts)**

Append to `DECISIONS.md` (statuses/wording adjusted to the actual accepted recommendations):

```markdown
### D-0015: Accept 0060-transport-abstraction report
**Date**: 2026-08-14
**What**: [the accepted recommendation shape and named tools]
**Why**: [one-line rationale from the report]
**From**: tracks/0060-transport-abstraction/report.md
**Affects**: 0060, 0070, 0050

### D-0016: Accept 0070-state-concurrency report
**Date**: 2026-08-14
**What**: [the accepted recommendation shape and named tools]
**Why**: [one-line rationale from the report]
**From**: tracks/0070-state-concurrency/report.md
**Affects**: 0070
```

Flip both README rows to `accepted`; frontier:

```markdown
> **Frontier:** Wave 2 accepted (D-0015, D-0016) — next: schedule spikes or the 0090 horizon scan.
```

Run: `node scripts/check-docs.ts`; commit:

```bash
git add DECISIONS.md README.md
git commit -m "feat(wave-2): record 0060/0070 acceptances (D-0015, D-0016)"
```
