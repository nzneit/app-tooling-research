# 0160-flight-recorder — acceptance-gate review record (2026-08-23)

The two-lens pass that runs at an acceptance gate, per **D-0033** (which ratified it) and **D-0037**
(which wrote it down at [`.claude/skills/gate-review/SKILL.md`](../../.claude/skills/gate-review/SKILL.md)),
run over [report.md](report.md) before the user's acceptance call. It is not a per-track obligation and
this file does not create one; it exists because the pass ran and its runners-up must be preserved
rather than discarded (the skill's own rule, and the repair for seed-corpus category J).

**This record does not accept the track.** Acceptance is the user's, recorded as a `D-####` entry. What
this pass produced is a corrected report and the list below of what was wrong with the previous one.

## What ran

Seven differently-framed lanes plus a completeness critic, each falsification lane followed by an
adversarial refuter, on the skill's two recorded practices: *framing diversity beats reviewer count*,
and *put findings to a refuter before they count*.

| Lens | Lanes | Raised | Survived |
|---|---|---|---|
| 1 — falsification | seam ownership · coverage · assumptions & propose-never-assert · arithmetic re-derivation · deviations & citation integrity | 37 | 22 |
| 2 — machinery | process stages · validator and conventions | 17 | 7 |
| Completeness critic | the lens nobody ran | 8 | 8 |
| **Total** | 14 agents | **62** | **37** |

**Deviations in this pass, stated because lens 1 check 5 demands it of every artifact:**

- **The critic's eight findings did not go through a refuter** — it ran last, by design, and nothing
  followed it. I verified its load-bearing premises by hand instead (D-0007's paragraph limit, the
  interface's missing redaction mode, the absence of any non-MQTT rate in the report, the 12 MiB ÷ 50 KB
  arithmetic). That is weaker than a refuter and is recorded as weaker.
- **External sources were not re-fetched.** D-0034 defers citation staleness; the deviations lane checked
  only internal consistency of external claims, which is how it found the Bugsnag/Faro pinning defect.
- **I refused none of the 37 survivors.** Every one was applied. That is an unusual adjudication rate and
  it should be read with the refutation rate beside it, not on its own: 25 of the 62 raised findings were
  killed before reaching me, and five of the survivors turned out to be the same defect found by five
  different lanes. I hand-verified eleven of the survivors against their cited sources before editing
  (listed at the end).

## Lens 1 — falsification: what it found

The six checks, and how each came out.

**1. Seam ownership** — three real breaks, all against accepted sibling artifacts, all now corrected in
the report:

- The worked example detected an MQTT reconnect with `snap.connection === 'reconnected'`, **a value
  0060's accepted `BoundarySnapshot` union does not contain**. The synthetic gap record — the thing that
  stops a bundle splicing pre- and post-reconnect history into one unbroken run, and the bridge to 0070's
  replay shape — was wired to a state that never occurs. The failure was invisible by construction: a
  bundle with no gap records looks exactly like a session with no reconnects. Now derived from an observed
  *transition*, comparing against the adapter's own last-seen value, because 0060's O6 permits snapshot
  coalescing.
- The abort counters proposed to 0060 were placed on "the existing `stats` surface", which 0060 does not
  have — in the same sentence that correctly called the boundary stats counter *ratified language with no
  design*. A proposal addressed to another track's owners has to describe their real surface.
- The report claimed to **share** 0050's redaction rule set while specifying an incompatible shape. 0050's
  accepted sketch types `patterns?: RegExp[]` with no `ruleId` — which is precisely the `lastIndex` hazard
  0160 diagnosed and ruled out, sitting unmarked in an accepted surface. Sharing is now stated as a
  *proposal* to 0050's owners, which is what lens 1 check 6 requires.

**2. Contradictory scoring** — the ownership split on the MQTT quarantine ride-along. The report had app
code populate it *after* the seal while assigning its redaction, its byte cap, and its boundedness property
test to the seal — so the one section no projection scrubber has ever touched sat outside the only component
that owns a rule set and a meter. Resolved by injection: the composition root supplies `quarantineSource`,
the recorder pulls at seal, and I6 is restated as *reads only what the composition root injects*.

**3. Assumptions declared** — two undeclared dependencies, both on the backend, both load-bearing:

- **Backend deduplication** was asserted as a property of a service that does not exist yet, in two
  different key shapes, and three client-side rulings rest on it: at-least-once retry, the acceptance of
  two near-duplicate bundles instead of cross-tab coordination, and the durable-parking refusal. Now a
  declared assumption and an intake ruling item.
- **The loss-marker route.** The report said "intake item c gains the question" — item c was answered and
  gained nothing; the ask existed only in that sentence. Since a beacon's response is unobservable, an
  endpoint with no route for the marker discards the entire compensation for refusing durable parking *and
  the client can never learn it*. Now raised properly as
  [intake 2026-08-23-0160-endpoint-contract](../../intake/2026-08-23-0160-endpoint-contract.md).

**4. Coverage** — two in-scope items the report had dropped silently. The plan's serialize-walk hazard says
app code in the walk can "throw, **stall, or mutate**"; the report answered the throw and said nothing about
the other two, while the design-level register carried both as `UNOWNED`. And the plan named the **RAM-disk
neutralization of every SDK's persistence feature** as a weighing element to price *before any candidate is
compared* — the words "offline" and "persistence" appeared nowhere in the report. Both now stated: a stall is
a residual hazard with no cheap client-side bound and a p99 measurement in the spike, and I6's "observation
only" is qualified to *in its outputs*, because the walk reads live app objects and a `project()` row is
handed the caller's live entry.

**5. Deviations complete and honest** — the one sentence in the report whose entire job is to disclose a
verification deviation was itself false. It said Bugsnag's citations were branch-pinned "unlike every other
candidate here"; **Faro's whole source-verified section is read on `main`**, six days ahead of the 2.10.0 tag
it is attributed to, and the tracekit and mnemonist citations are on `master`. Corrected with the accurate
list.

**6. Propose, never assert** — clean apart from the 0050 rule-set sharing (check 1) and the 0060 `stats`
surface (check 1). The report's D-0006 reading, its egress-policy-row route, and its abort-counter proposal
all correctly propose rather than assert.

**Plus the arithmetic lane**, which is where the skill's *re-measure load-bearing numbers* practice earned
its place:

- The non-MQTT byte term did not reproduce. "About **1.5 MB**" against operands the same sentence states
  (50 HTTP records × 10 KB + 100 transitions × 4 KB + 50 log lines × 1 KB) is **950 KB** — a 58%
  overstatement, already propagated into `facts/app-profile.md`. Corrected in both, with the counting rule
  now printed beside the number.
- Risk 6's "**under 2%** of device RAM" — the figure that disposes of D-0043's residual — counted rings
  only, omitting the two largest transient holders the same report designs: the seal peak and the two
  delivery slots per tab. At the low end of the stated RAM range the real figure is about **3.8%**. Still
  comfortable; not what it said.
- I3's envelope formula has three terms and the worked table's only justification printed one. `headerAllowance`
  appeared in the formula, had no config field and no value, and the quarantine cap had neither either —
  while the 50 MB wire ask leaned on both. Both now exist with defaults, and the residual is printed: the
  table validates while `headerAllowance + 1,400 × PER_RECORD_OVERHEAD ≤ 1 MiB`.
- The Runtime overhead row compared a deps-only figure, an error-only-subset figure, and two whole-package
  figures as though they were commensurable, and the one number that made two candidates look equal has no
  source anywhere in the track. Each cell now names what it measures, and the unsourced one says so.

## Lens 2 — machinery: what the process let through

Coverage-forcing enumeration over the stages this track passed through, per D-0036 — no scoring, no RPN,
one severity label. Seven survived. What they have in common is worth more than the list: **every one is a
claim that no validator can see**, and three are claims about the repo's own record rather than about the
app.

- **A number that disagreed with three sibling artifacts.** The report said four of the twelve intake answers
  settled less than their question asked; the intake register, `README.md` and **D-0042 — the decision minted
  from this very round** — all say six, and the report's own assumption list carried more than six. Five of
  the seven lanes found it independently, which per the skill's own note tracks how *cheap it was to detect*,
  not how much it mattered. It matters mostly as a demonstration: D-0042 exists because under-answered items
  read as closed, and the report understated its own assumption debt in exactly that number.
- **An answer that is truncated mid-sentence.** Intake item j reads "They have distinct views and distinct" —
  and stops. D-0042's clause-by-clause check tests the *question's* clauses for coverage; nothing tests the
  *answer text* for completeness, so a sentence that simply stops passes as an answer. The report had read it
  as settled and load-bearing. Now carried as an assumption, and the intake file's header note names it as a
  seventh partial.
- **An answer filed in the wrong place.** Item h's answer sits below the "Items i–l added" divider, so h reads
  as unanswered in place while its answer floats orphaned in the next section's preamble. Moved.
- **A register whose line citations have all moved.** The design-level FMEA register cites `report.md:NNN`
  about 199 times against the pre-revision report; 34 of the 40 mechanically checkable ones now land outside
  the question they name, and a few point at text that changed *because of the finding citing it*. The report
  offers that register as the way to check 24 of its own sentences. A checker for this was measured against
  the whole corpus per D-0037's rule and scored **zero true hits**, so it is a do-not-build; the repair is a
  header note on the register, which is now there.
- **A pass with no register.** The frontier credits a three-lane verification pass with 33 corrections to the
  report under acceptance and names three of its findings — and no artifact in this repo records that pass.
  Its only trace is a commit subject. At a gate, nobody can check whether those three were fixed correctly or
  whether the other 30 were real. The frontier's claim is corrected to what is actually recorded; this file
  is the precedent going the other way.
- **An always-loaded surface that grew 37× unpriced.** `README.md`'s frontier line is **12,510 characters —
  75% of the entry-point file** — up from 336 on 2026-08-14, and this track added 6,584 of them. D-0029 makes
  always-loaded bytes ceiling-free but requires them **priced and reported**; the 9900 report cut twelve
  controls under that discipline, and D-0042 priced its own 55-word bullet the same day this frontier grew.
  `checkFrontier` asserts the line exists and nothing else, so it is the check that appears to govern this
  surface while bounding nothing. Raised as a ruling with options at
  [intake 2026-08-23-frontier-bound](../../intake/2026-08-23-frontier-bound.md), because a threshold is the
  user's call, not a checker's to invent.
- **A decision declined on a count that expired.** D-0037 cut a repair because "the research-plan template has
  zero remaining authorings, so changing it propagates into nothing". Two plans were authored within days —
  0150 and 0160 — and both inherited the dead field the corpus records as category L. Corrected in place in
  D-0037 and in the corpus. The transferable rule is narrow and worth having: **a decision declined on a count
  needs the count's expiry condition recorded beside it**, because "nothing more will be authored" is falsified
  by the program simply continuing.

**Do-not-build register.** Five candidate validator checks were measured against the corpus per D-0037's rule
before any was proposed. Four scored zero and are refused: a stale `path:LINE` citation checker (259 cites
scanned, 212 resolvable, 0 true hits), a dangling-`Q<n>` checker, an intake item-structure checker, and a
cross-file numeric-agreement checker. The fifth — a character cap on the frontier — scores exactly one hit at
any threshold between ~700 and 12,510 characters, which is why it is the only one raised, and raised as a
ruling rather than built.

## The completeness critic — the lens nobody ran

Eight findings, and the three that moved claims rather than wording:

- **The Recommendation prescribed a mechanism the report's own question 3 had falsified.** It adopted
  safe-stable-stringify "wrapped so its in-band elision becomes structured markers" — while question 3 proves
  that wrapping cannot work (a `replacer` never sees breadth elision, because the library sorts and *slices*
  before iterating; and its marker strings are indistinguishable from app strings with the same content). The
  bill of materials called itself "verified" while question 15 calls the same open question *the finding that
  most affects the build's bill of materials*. The adoption is now explicitly conditional, in the STE, the
  recommendation line, the survey bullet, and question 14.
- **Risk 2's mitigation could not be expressed in the report's own interface.** The conservative redaction
  default — skeleton payloads, leaf values redacted, per-bucket verbatim exemptions — is what stands in for the
  disclosure policy nobody has ruled. `RedactionRules` offered three match lists and no skeleton mode;
  `BucketPolicy` had no exemption bit; and the worked configuration, the one every byte figure is computed
  against, configured a **two-key denylist that would have shipped every other leaf value verbatim to a cloud
  endpoint**. Fixed by adding the two fields and correcting the worked example, with a spike assertion so the
  default is tested rather than asserted.
- **The cross-bucket window is the minimum of four spans, and only one was computed.** The recorder's one
  distinct claim is interleaved history; the report applied its own "a count is not a window" standard to the
  single bucket where a rate happened to exist, raised that bucket twelvefold on the strength of it, and never
  stated or declared-unknown the other three rates. Twenty-four seconds of MQTT interleaves with nothing for
  the twenty-two seconds the other three do not reach. Now a paragraph in question 2 and a declared assumption.

Also from the critic and now corrected: the STE ran to four paragraphs against D-0007's **max 2** (the only
report in the repo that does, and the validator checks only that the heading exists), and ranked its risks
differently from the body, which twice calls a *third* risk the design's largest coverage hole; risk 1's
mitigation claimed "every piece copies a cited design" when the report's whole case for building is that
nothing in the field does byte-bounded serialize-at-capture, so the pieces at the centre of that risk are
novel by the report's own finding; the rationale's owned-line estimate omitted every piece the report calls
non-trivial and claimed property-test coverage for the one chunk no lane this repo owns can reach; the rubric's
evidence-asymmetry note named two asymmetries and then certified as symmetric the row carrying the largest one;
and the MQTT row's "~24 s" is true only at typical payload size — its byte cap binds first, giving ~5 s at item
a's worst case.

## What this changes about the verdict

**Nothing, and the report is meaningfully different anyway.** The recommendation is still build, on the same
two legs: the hard requirements are owned work under every posture, and the strongest adoptable core holds live
caller references in its history buffer. Neither leg was touched by any finding. What changed is the honesty of
the surround — one of two replay pre-commitments withdrawn, one dependency made conditional, one mitigation made
buildable, three assumptions declared, two intake files raised, and four numbers corrected.

One finding is worth flagging to whoever runs the spike rather than to whoever accepts the report: **the
elision mechanism now sits under the recommendation as well as in the spike list.** If the wrapper must own the
walk, the build's bill of materials loses a dependency and gains a component, and that is a bigger swing than
anything else on the list.

## Verified by hand before editing

Eleven survivors were checked against their cited sources directly rather than taken from the lane: 0060's
`BoundarySnapshot.connection` union (design.md:296) and `BoundaryFetcher`'s return type (design.md:309-313);
`BoundaryError.status` on the reject path only (design.md:389); 0060's O6 coalescing rule and its
`quarantine.entries()` surface; 0060's design.md:245-252 against 0070's, for the ambiguous citation; 0050's
`RedactionRules` (report.md:133); D-0007's "max 2 paragraphs"; the 950 KB re-derivation; the frontier's
character counts and their git history; the sixteen research plans against nine accepted tracks; and intake
item j's truncation, which I had noticed independently while reading the file.

---

# Appendix — all findings, verbatim

Preserved per the skill's rule: *do not discard the runners-up*. Findings appear as the lanes returned them,
including the ten machinery findings the refuter killed, each with its reason. Severity is post-refutation.

One mechanical edit, disclosed because "verbatim" should mean it: three quoted excerpts reproduced a relative
markdown link from the intake file they were quoting, which resolves from that file's directory and not from
this one. The link target — and only the target — is replaced with a placeholder so the doc validator's link
check passes. No finding text is otherwise altered, including the ones that turned out to be wrong.

### Lens 1 — falsification (22 survived)

#### SEAM-1 — The report asserts that the HTTP bucket's record is "losslessly projectable to HAR 1.2" while the record shape it specifies carries neither a concrete URL nor a status field, and the tap seam it chose (0060's `BoundaryFetcher`) structurally cannot supply the status on the success path — so a charter-named reserved decision (D-0040 item 5, HAR-projectability) is claimed to hold when it does not.

- **Severity (post-refutation)**: blocker  ·  **Lane**: seam
- **Evidence**: tracks/0160-flight-recorder/report.md:731 — "The two format-level pre-commitments hold and are now design columns: the HTTP bucket's record fields are chosen to be losslessly projectable to HAR 1.2 entries (the regression path HAR → @mswjs/source `fromTraffic` → MSW handlers…)". Contradicted three ways. (a) report.md:81 — the HTTP scrubber "captures template-key identity (`GET /v1/plants/{plantId}`), status, and bounded bodies", i.e. deliberately not the concrete URL that `fromTraffic` keys handlers on. (b) report.md:520-542 — `CapturedRecord` declares seq/gseq/wallMs/monoMs/sourceId/kind/direction/phase/outcome/correlationId/mimeType/encoding/bytes/body/markers and no url, status, or headers member. (c) report.md:90 — the outcome-record is said to carry "status or `BoundaryError` class, duration, bounded body", but the seam it wraps is tracks/0060-transport-abstraction/spikes/boundary-wiring/design.md:309-313 — "export type BoundaryFetcher = <TOk>(req: {…}, opts?: { signal?: AbortSignal }) => Promise<Validated<TOk>>" — which resolves a validated body and no Response, so no status exists on the 2xx path; and report.md:90 rejects the one alternative that would see it ("The `FetchLike` adapter decoration alternative is **rejected on verified grounds**"). The track's own supporting enumeration already recorded this at high severity and it was not absorbed: tracks/0160-flight-recorder/design-fmea-enumeration.md:1734 — "HAR projectability is falsified by the template-key identity choice: the record has no URL, no headers, and no typed status."
- **Why it matters**: D-0040 ruled that replay/regression-generation's "entire claim on this track is the reserved-decisions checklist", and HAR-projectability of the HTTP bucket is one of the two named format-level pre-commitments. A pre-commitment is cheap now and unrecoverable later; recording it as satisfied closes the door it was supposed to hold open, and the fix is not editorial — obtaining a status or a concrete URL means either changing the tap seam (which the report rejected on 0060's two-adapter rule) or amending the privacy/cardinality decision made in question 3.
- **Proposed fix**: Stop claiming the pre-commitment holds. Either (a) downgrade the sentence at report.md:731 to state plainly that HAR projection is currently blocked by two named choices — template-key identity (no concrete URL/params) and the mutator seam's inability to see response status or headers — and record what each would cost to reverse; or (b) add the fields and say where they come from: a concrete-URL field plus a `status` field on `CapturedRecord`, with the status sourced from a boundary change 0060 would have to ratify (the same proposal route question 5 uses), since `BoundaryFetcher` cannot yield it today.
- **Refuter**: Survives on all three legs, and nothing in the report resolves it. report.md:731 does say "The two format-level pre-commitments hold and are now design columns: the HTTP bucket's record fields are chosen to be losslessly projectable to HAR 1.2 entries (the regression path HAR -> @mswjs/source `fromTraffic` -> MSW handlers...)". (a) report.md:81 confirms the HTTP scrubber "captures template-key identity (`GET /v1/plants/{plantId}`), status, and bounded bodies" - no concrete URL, no params, no headers; grepping url/URL across the report finds no field carrying a concrete URL anywhere. (b) `CapturedRecord` (report.md:520-542) declares seq/gseq/wallMs/monoMs/sourceId/kind/direction/phase/outcome/correlationId/mimeType/encoding/bytes/body/markers - checked line by line; no url, status, or headers member. (c) 0060 spike design.md:309-313 types `BoundaryFetcher` as `=> Promise<Validated<TOk>>` and design.md:466-475 shows the caller-owned mutator as `boundary.fetcher<T>(req, opts)`, so the wrap sees a validated body and no Response; `BoundaryError.status` (design.md:389) exists only on the reject path, so a 2xx entry has no status - the unqualified "status" at report.md:81 overstates what the chosen seam yields, and the alternative seam is refused at report.md:90 ("The `FetchLike` adapter decoration alternative is **rejected on verified grounds**"). The track's own design-fmea-enumeration.md:1734-1737 raised exactly this at high severity and no version of it was absorbed - while the parallel MQTT pre-commitment WAS corrected honestly in the same sentence (packetId, report.md:81/731), showing the absorption pass ran and skipped this one. HAR-projectability is a charter-named reserved decision (DECISIONS.md:309, D-0040 item 5), so a false "holds" in the report's own reserved-decisions ledger is blocker-class, not editorial.

#### SEAM-2 — Ownership of the MQTT quarantine ride-along is split incoherently: the report assigns its population to app code (the transport adapter, after the seal, outside the recorder) while assigning its redaction, byte cap, and boundedness property tests to the recorder's seal — so the section most in need of scrubbing is produced where the recorder's rule set structurally cannot run.

- **Severity (post-refutation)**: major  ·  **Lane**: seam
- **Evidence**: Population is app-side and post-seal: report.md:89 — "at assembly time the **transport adapter** — app code, not the recorder, which reads no host surface (I6) — copies `quarantine.entries()` into the bundle's own quarantine section"; report.md:464-466 — "It is filled by the transport adapter at assembly time from the boundary's own read-only `quarantine.entries()` — not by the recorder, which reads no host surface (I6) — and it is BYTE-CAPPED and marked". Yet redaction and the cap are recorder-side: report.md:739 — "**the seal applies the same rules to the envelope's own free text** — the trigger's error message and stack…, the caller-supplied `context`, the health block's caller-derived strings…, and the quarantine ride-along, which is by definition traffic no schema accepted, so no projection scrubber ever ran over it"; report.md:206-209 — "this one covers bucket projections AND, at seal, the fields that bypass them: the trigger's error/stack, the caller context, and the quarantine ride-along"; report.md:134 — "the quarantine ride-along is byte-capped separately because it enters the bundle without passing the meter"; report.md:829 lists "the byte-capped quarantine ride-along under oversized rejects" among the recorder's own fast-check boundedness properties. The type placement compounds it: the field is declared on the fully-`readonly` `SealedBundle` (report.md:459-476), which report.md:457 calls the "input to the transport seam" — i.e. the recorder's output.
- **Why it matters**: Quarantined traffic is by definition unvalidated raw payload, and risk 2 states no disclosure policy has been ruled, so the conservative redaction default is load-bearing. As written, no component provably applies it: the recorder never sees the section, and the transport adapter is given no rule set, no marker discipline, and no cap mechanism. The same split makes I3's byte cap and the named spike property unrunnable against the recorder.
- **Proposed fix**: Pick one owner and say so in all four places. Cheapest consistent version: the composition root injects a `quarantineSource: () => readonly QuarantineEntry[]` into `RecorderConfig`, so the recorder pulls, projects, redacts, meters, and caps the section inside the seal — keeping I3, Q13 and the spike property true — and I6's "reads no host surface" is restated as "reads only injected suppliers". Otherwise move the cap, the redaction, and the spike property to the transport adapter and delete the ride-along from `SealedBundle`.
- **Refuter**: The four placements are quoted accurately and cannot all be true. Population is explicitly non-recorder: report.md:89 "at assembly time the **transport adapter** - app code, not the recorder, which reads no host surface (I6) - copies `quarantine.entries()` into the bundle's own quarantine section", repeated in the type comment at report.md:462-469 ("It is filled by the transport adapter at assembly time ... not by the recorder"). Yet report.md:739 puts it inside the seal ("**the seal applies the same rules to the envelope's own free text** - ... and the quarantine ride-along, which is by definition traffic no schema accepted, so no projection scrubber ever ran over it"), report.md:206-209 repeats it inside `RecorderConfig`'s own doc comment ("AND, at seal, the fields that bypass them: ... and the quarantine ride-along"), I3 at report.md:134 assigns it a byte cap as a recorder invariant, and report.md:829 lists "the byte-capped quarantine ride-along under oversized rejects" among the recorder's fast-check boundedness properties. The type placement decides it: the section is a fully-`readonly` member of `SealedBundle` (report.md:459-476), which report.md:457 labels "input to the transport seam" - the recorder's output - typed as `readonly entries: readonly CapturedRecord[]` with recorder-shaped accounting (`bytes`, `truncatedEntries`, `droppedForBytes`), so the adapter cannot be its filler. I tested the two-senses-of-"assembly" escape (report.md:687 lists assembly as a post-seal async step; report.md:697 calls the transport adapter "app code assembling the OpenAPI wire body from the `SealedBundle`") and under either sense one of the quoted sentences is false. Load-bearing because risk 2 (report.md:787) states no disclosure policy has been ruled, so the conservative redaction default is doing real work over exactly this unvalidated content. Major, not blocker: the recommendation and rubric verdict are untouched.

#### SEAM-3 — The worked example detects an MQTT reconnect by testing `snap.connection === 'reconnected'`, a value that does not exist in 0060's accepted `BoundarySnapshot.connection` union — so the synthetic gap record the report calls load-bearing would never be emitted.

- **Severity (post-refutation)**: major  ·  **Lane**: seam
- **Evidence**: tracks/0160-flight-recorder/report.md:651 — "if (snap.connection === 'reconnected') mqttTap.record({ gap: true }, { kind: 'gap' });". The accepted sibling type is tracks/0060-transport-abstraction/spikes/boundary-wiring/design.md:296 — "readonly connection: 'idle' | 'connecting' | 'connected' | 'reconnecting' | 'degraded' | 'ended';" (`'reconnected'` appears nowhere in that file; the neighbouring line report.md:645 correctly uses `'ended'`). The mechanism is not decorative: report.md:94 — "wire 1 carries no gap concept, so without an explicit marker a bundle splices pre- and post-reconnect records into unbroken history with every drop counter at zero — the adapter therefore records a synthetic gap record from its wire-2 subscription", and report.md:731 makes the same gap records "what makes the stream convertible to the `ValidatedMessage | 'gap'` shape 0070's `RaceHarness.push` consumes".
- **Why it matters**: The gap record is the only thing standing between a bundle and a silently spliced history across a reconnect, and it is the sole bridge to 0070's replay harness — the second of D-0040's two format-level pre-commitments. Wired to a non-existent state it is dead code, and the failure is invisible (a bundle with no gap records reads exactly like a session with no reconnects).
- **Proposed fix**: Derive the gap from an observed transition rather than a state value: keep the previous `connection` in the wire-2 closure and record the gap when it moves into `'connected'` from `'reconnecting'` (or when `snap.attempt` returns to 0 after being non-zero). Note in the same comment that 0060 O6 (design.md:501) permits coalescing of snapshot notifications, so the adapter must compare against its own last-seen value rather than assume every intermediate state is delivered.
- **Refuter**: Verified verbatim. report.md:651 reads `if (snap.connection === 'reconnected') mqttTap.record({ gap: true }, { kind: 'gap' });` while 0060 spike design.md:296 types the field as `readonly connection: 'idle' | 'connecting' | 'connected' | 'reconnecting' | 'degraded' | 'ended';` - I grepped the entire 0060 design and 'reconnected' appears nowhere, while the adjacent report.md:645 uses `'ended'` correctly, so the rest of the sketch is drawn from the real union. This is the report's only concrete specification of gap emission: the prose at report.md:94 ("the adapter therefore records a synthetic gap record from its wire-2 subscription (worked example above)") and report.md:731 (the same records are "what makes the stream convertible to the `ValidatedMessage | 'gap'` shape 0070's `RaceHarness.push` consumes") both defer to this line rather than restating the trigger condition, and report.md:73's byte arithmetic assumes the records exist. As written the branch is unreachable and the failure is silent. Major: correctable in place, does not move the recommendation.

#### SEAM-4 — Question 5 proposes putting three abort counters on "the existing `stats` surface" of 0060's boundary — a surface that does not exist in 0060's accepted design — contradicting the same bullet's own statement that the boundary stats counter is ratified language with no design.

- **Severity (post-refutation)**: minor  ·  **Lane**: seam
- **Evidence**: tracks/0160-flight-recorder/report.md:102 — "visibility via a \"boundary stats counter\" that remains ratified language with no design (DECISIONS.md:147-152)" … "the stats counter as three monotonic integers on the existing `stats` surface (`aborted`, `timedOut`, `settled`), read at bundle-assembly time by the transport adapter into the wire body's environment block". 0060's accepted handle (tracks/0060-transport-abstraction/spikes/boundary-wiring/design.md:220-262) exposes only `actor`, `subscribe`, `publish`, `fetcher`, `quarantine`, `start`, `reconnect`, `dispose`; the sole occurrence of the word in that file is the gate note at design.md:587 — "a boundary stats counter carries visibility" — i.e. the ratified phrase itself. (0070's `IngressStats`, design.md:245-252, is a different track's ingress accounting and is cited separately in the same sentence as "the same route question 9 uses for `IngressStats`".)
- **Why it matters**: The proposal is addressed to 0060's owners, so it must describe 0060's real surface: as written it reads as a small addition to something already shipped, when it is in fact a new public member on an accepted interface (with the wire-2 `depths` block as the nearer precedent). It also makes the bullet self-contradictory — no design exists, yet the surface is "existing".
- **Proposed fix**: Rewrite as: "a new read-only `stats: { aborted: number; timedOut: number; settled: number }` member on the boundary handle — or three fields alongside `BoundarySnapshot.depths` on wire 2 — read at bundle-assembly time by the transport adapter", and keep the note that whether to add it is 0060's call.
- **Refuter**: Factually correct but overstated. report.md:102 does say "the stats counter as three monotonic integers on the existing `stats` surface (`aborted`, `timedOut`, `settled`)" in the same bullet that calls it "ratified language with no design (DECISIONS.md:147-152)", and D-0018 (DECISIONS.md:149) indeed only says "visibility via a boundary stats counter". Grepping 0060's spike design and 0060's report for `stats`: the sole hit in the design is the gate note at design.md:587, and the accepted handle (design.md:220-262) exposes only actor/subscribe/publish/fetcher/quarantine/start/reconnect/dispose - so no `stats` surface exists and "existing" is wrong. Downgraded to minor because nothing turns on it: the same bullet already states the counter has no design, routes the decision to 0060's owners ("Whether to build it is 0060's call"), and says the recorder is complete without it; the repair is one word with no design consequence.

#### SEAM-5 — The report claims to share 0050's redaction rule shape while specifying an incompatible one — 0050's accepted sketch types patterns as live `RegExp[]` with no rule id, which is exactly the hazard 0160 rules out — and never records that 0050's accepted surface must be amended.

- **Severity (post-refutation)**: minor  ·  **Lane**: seam
- **Evidence**: tracks/0160-flight-recorder/report.md:206 — "ONE rule set for the WHOLE bundle (0050's rule shape, shared as rules …)" and report.md:739 — "the applier is owned, shared with the facade when the facade lands". 0160's own shape refuses that form: report.md:270-283 — "Rules are DATA, never live RegExp objects (amended 2026-08-23 …). Patterns are therefore source+flags strings: the validator rejects the `g` and `y` flags outright", with `patterns?: { source: string; flags?: string; ruleId: string }[]`. 0050's accepted sketch is tracks/0050-logging/report.md:133 — "export interface RedactionRules { keys?: string[]; paths?: string[]; patterns?: RegExp[] }" — live RegExp objects, and no `ruleId` anywhere, which RD-10's `{redacted, ruleId}` markers require.
- **Why it matters**: Two accepted artifacts cannot both own one rule set in two incompatible shapes; whichever ships second silently redefines the other. Worse, the `lastIndex` defect 0160 diagnosed ("half the fleet's records ship the secret while the other half carry {redacted} markers") lives verbatim in 0050's accepted `patterns?: RegExp[]`, and the report — which cites 0050 as the shape's origin — routes no correction back to it, so the defect stays in the accepted logging surface unmarked.
- **Proposed fix**: State the divergence explicitly in question 13: 0160 owns the data-form `RedactionRules` (source+flags+ruleId, g/y rejected), 0050's `patterns?: RegExp[]` carries the lastIndex hazard and needs the same amendment plus a `ruleId` before the two can share one value, and raise that as an item to 0050's owners (or as an intake ruling item) rather than describing the shape as already shared.
- **Refuter**: Quotes check out - 0050 report.md:133 is `export interface RedactionRules { keys?: string[]; paths?: string[]; patterns?: RegExp[] }` with no ruleId, 0160 report.md:279-283 declares `patterns?: { source: string; flags?: string; ruleId: string }[]` under the amendment note at 270-278, and report.md:206 does call this "0050's rule shape" while report.md:739 says the applier is "shared with the facade when the facade lands"; nothing in the artifact set records that 0050's sketch needs amending. But two things cut the severity: the report never claims byte-compatibility (report.md:27 says "The recorder shares 0050's fast-redact-*style* rule set as data, with an owned applier") and it redefines the divergent member in full, in place, with its reason - no reader is misled about which shape 0160 recommends; and 0050's facade is unbuilt with its surface explicitly a signature sketch under D-0001 (report.md:92, citing D-0014), so no shipped surface is being silently redefined. What genuinely remains is that the lastIndex hazard 0160 diagnosed sits unmarked in a sibling's accepted sketch with no correction routed - a sentence in question 13 or an intake item, i.e. minor.

#### SEAM-6 — The Facts section states that four of the twelve intake answers settled less than their question asked; the intake register and the accepted ledger entry that was minted from this very round both say six, and the report's own "Still assumed" list enumerates more than four.

- **Severity (post-refutation)**: major  ·  **Lane**: seam
- **Evidence**: tracks/0160-flight-recorder/report.md:796 — "Four of the twelve answers settled less than their question asked, and each is carried below rather than read as closed." Contradicted by intake/2026-08-22-0160-recorder-facts.md:11 — "**Six answers did not fully close their questions**, and the report carries each as a declared assumption" (listing b, a, c, e, i, h), and by DECISIONS.md:325 (D-0042) — "Twelve items came back answered; **six settled less than their question asked**, and four of those were initially read as closed". The report's own list at report.md:815-824 then carries partials for a, e, i, c, part of d, h, g and k — eight, not four.
- **Why it matters**: This is the one paragraph a reader uses to audit D-0004 compliance — how much of the design still stands on assumption. Understating it by a third, in the exact number D-0042 was minted to protect, invites the same failure D-0042 exists to prevent (an under-answered item read as closed). The "four" also looks like the wrong half of D-0042's sentence copied across.
- **Proposed fix**: Change to "Six of the twelve answers settled less than their question asked (items a, b, c, e, h, i — four of them initially read as closed), and item d's xstate half went unanswered", so the count matches the intake header and D-0042, and the list below it.
- **Refuter**: Confirmed against all three sources. report.md:796: "Four of the twelve answers settled less than their question asked, and each is carried below rather than read as closed." intake/2026-08-22-0160-recorder-facts.md:11: "**Six answers did not fully close their questions**", enumerating six (b, a, c, e, i, h) at lines 14-27. DECISIONS.md:325 (D-0042): "Twelve items came back answered; **six settled less than their question asked**, and four of those were initially read as closed" - so "four" is the wrong half of that sentence. The report's own "Still assumed" list at report.md:815-824 then carries partials for a, e, i, c, part of d, h, g and k, contradicting the count in the paragraph directly above it. Major rather than blocker: every partial is in fact carried below with its consequence, so no assumption is hidden - only the count is wrong - but it is wrong in the paragraph that audits D-0004 compliance and in the exact number D-0042 was minted over.

#### COV-1 — The report announces it absorbed plan question 3's four enumeration hazards, but hazard 2 — the serialize walk can "throw, stall, or mutate" — is answered for throws only; the stall and mutate thirds are dropped without a word, and the design register marks the mutate half UNOWNED against the report's own I6.

- **Severity (post-refutation)**: minor  ·  **Lane**: coverage
- **Evidence**: research-plan.md:215-218 states the hazard: "**the serialize walk executes app code** — getters, Proxy traps, and `toJSON` run inside the capture turn, can throw, stall, or mutate (a revoked immer draft throws mid-walk; BigInt throws in plain `JSON.stringify`), so the projection guards against the walk itself, not only value shapes". report.md:77 claims absorption: "The projection contract, absorbing the four enumeration hazards:". report.md:79 answers one verb: "1. **The walk is guarded, not just the shapes.** ... The projection wraps the entire walk in a try/catch whose failure path produces a counted projection-failure drop with the entry's bucket, sequence number, and error class". No text anywhere covers a getter/Proxy trap that stalls rather than throws — report.md:723 instead asserts completeness in the other direction: "The enumeration found the one capture-path step this report had not budgeted: redaction." And nothing covers the recorder→app direction; report.md:82 handles only the reverse ("**Sibling mutation is accepted and stamped, not fought**"). The design register contradicts both: design-fmea-enumeration.md:500-501 (capture-path-14) — "I6's 'observation only' is broken by the projection itself." / "- **Owner**: `UNOWNED`", and design-fmea-enumeration.md:514-517 (capture-path-16) — "`project()` is handed the caller's live entry and can mutate it, making a config row able to alter the traffic the boundary is still dispatching" / "- **Owner**: `UNOWNED`". Neither appears in the 28-amendment table (design-fmea-enumeration.md:45-74) nor in "What was not acted on, and why" (design-fmea-enumeration.md:92-110), which disposes of the 148 *partial* modes, charter-refusal modes, severity inflation and residual unknowns — not these. Meanwhile the deliverable interface still asserts the invariant unqualified at report.md:143-145: "I6  Observation only. The recorder subscribes to nothing, publishes nothing, classifies nothing, participates in no transaction, and never reports through itself."
- **Why it matters**: The claim "absorbing the four enumeration hazards" is a coverage claim a gate reads as discharged. Two-thirds of one hazard is not addressed, and the unaddressed part attacks I6 — the invariant on which the whole "the recorder never becomes the incident" argument rests. A memoizing getter or a reactive-store proxy read by the eager full-property walk changes app state and scheduling on the app's own dispatch turn, and a `project()` row can mutate the payload 0060 is still dispatching to other consumers; neither has a counter, a marker, or a spike lane, so a regression caused by the observer would be attributed to app code.
- **Proposed fix**: Add a fifth numbered point to question 3 that names the two unanswered thirds honestly: (a) a stall in app code during the walk is invisible to I1's try/catch and is not bounded — state it as a residual hazard and add "projection walk against getter-bearing/proxy-wrapped operands, measured for tail cost, not mean" to the question 15 spike list; (b) the walk and `project()` read and may mutate live app objects, so qualify I6 to "observation only in its outputs" and require `project()` to be pure (validator-unenforceable, so state it as a documented contract with the failure mode named). Then soften report.md:77 from "absorbing the four enumeration hazards" to "absorbing three and naming what remains of the fourth", and drop the "one capture-path step this report had not budgeted" claim at report.md:723.
- **Refuter**: Partly refuted. The finding's central claim — that the stall and mutate thirds are 'dropped without a word' — is false for stall. report.md:723 explicitly distinguishes the two verbs ('a stall, not a throw, so the try/catch never sees it and no drop counter records it'), report.md:83 budgets walk cost against a ~3%-of-a-core figure with plan B pre-named, and report.md:747, :788 and :828 all name the exact spike lane the finding proposes adding ('serialize live app-shaped objects (getter-bearing, Proxy-wrapped, immer-drafted) through the bounded projection ... on device-class hardware'). The design register itself rates the stall mode `partial:Q3/Q11`, not UNOWNED (design-fmea-enumeration.md:494-498), and its 'What was not acted on' section disposes of partials as 'the design working as intended ... a spike-measurable quantity open' (design-fmea-enumeration.md:93-98). One mutate direction is also covered: report.md:724 'Re-entrancy is refused and counted: the projection walk executes app code by design, and a getter that logs re-enters the recorder ... The guard is a flag; the loss is `drops.reentrant`.' The finding's use of report.md:723 as a completeness assertion is a misread — that sentence is scoped to cost budgeting of capture-path steps, not to I6. What survives, and only this: the recorder→app read-side-effect direction is genuinely absent (grep for getter/proxy/memoiz/side-effect across report.md returns only throw and re-entrancy contexts), and the `project()` row at report.md:258-263 states only that 'a throw is a counted projectionFailure' with no purity requirement, matching UNOWNED modes capture-path-14 (:500-501) and capture-path-16 (:514-517), neither of which appears in the 28-amendment table or the not-acted-on list. But I6 as written (report.md:145-147) constrains outputs — 'subscribes to nothing, publishes nothing, classifies nothing, participates in no transaction, and never reports through itself' — and the register concedes this ('I6 constrains the recorder's OUTPUTS ... it says nothing about the side effects of reading'), so 'the deliverable interface still asserts the invariant unqualified' overstates: nothing in I6's text is contradicted by a read-walk. Real residual, two-thirds wrong indictment: minor, worth one clause on project() purity and read side effects.

#### COV-2 — The plan requires the adopt+wrap comparison to price in the RAM-disk neutralization of every candidate SDK's offline/persistence feature; the report never mentions SDK persistence, Sentry's offline transport, or the neutralization anywhere — the words "offline" and "persistence" do not occur in the report at all.

- **Severity (post-refutation)**: minor  ·  **Lane**: coverage
- **Evidence**: research-plan.md:67-73 makes it one of three consequences that land "before any candidate is compared": "- **Every candidate SDK's offline/persistence feature is neutralized.** Sentry's offline transport is an IndexedDB queue ([offline.js, `dbName 'sentry-offline'`]...); 0050's blessed offline-buffer prior art is the same pattern. On these devices it survives nothing a memory buffer wouldn't, **which removes one of the standing reasons to adopt an SDK at all**". research-plan.md:474-476 makes it an explicit report obligation: "The report weighs both against the same rubric with the RAM-disk neutralization of SDK persistence and the replay-door schema ownership (question 12) priced in." research-plan.md:580 lists it among prior art to dispose of: "Sentry's envelope protocol and offline transport (the latter neutralized by the RAM disk)". In report.md, `grep -c 'offline'` and `grep -c 'persistence'` both return 0. The Sentry survey section (report.md:33-41) enumerates the wrap's verified mechanics and running costs without it; question 14 (report.md:743) divides the wrap into "(a) what the recorder must bypass ... (b) what the recorder must suppress ... (c) what is genuinely reusable" with no persistence term; the rubric (report.md:757-768) has no cell mentioning it. The report's only RAM-disk applications are to its own durable-parking refusal (report.md:711-713) and to risk 6 (report.md:791).
- **Why it matters**: The plan named this as a factor that removes a standing reason to adopt, and the charter (D-0040 ruling 4) made the RAM-disk premise binding on exactly this reasoning. The verdict is build, so the omission does not flip it — but the report presents the equal-merit comparison as complete ("Run honestly at equal merit, the comparison resolves on capture-side evidence", report.md:743), and a reader auditing the plan against the report finds a named weighing element silently absent. It also leaves the plan's disposal of Sentry's offline transport as prior art unclosed.
- **Proposed fix**: One clause in the Sentry survey section and one in question 14's (c) list: state that Sentry's offline transport (and every candidate's persistence feature) is an IndexedDB queue that on this fleet spends the same RAM as the in-memory ring, so it contributes nothing to the wrap's value and is not counted among what the wrap buys — citing plan lines 67-73 rather than re-deriving it.
- **Refuter**: Verified but overstated. The greps hold: case-sensitive 'offline' = 0, 'persistence' = 0, 'sentry-offline' = 0 in report.md; the single occurrence is 'Offline behaviour is drop-oriented' at report.md:49, about Faro, not about the RAM-disk neutralization. The Sentry section (report.md:31-41) and question 14's (a)/(b)/(c) division (report.md:743) carry no persistence term, and the rubric's ten rows have no cell mentioning it. The plan obligations quoted are accurate (research-plan.md:67-73, :474-476, :580). But the substance is not absent from the report, only from the comparison: report.md:713 states 'on this fleet IndexedDB is RAM (parking buys the tab-crash window only, never a power cycle)' and report.md:791 repeats it, so the premise is established and applied — just to the report's own parking refusal rather than to the SDKs'. The omitted factor argues *for* the verdict the report reached (build), so no claim is misrepresented and no verdict moves; question 14's other named element (replay-door schema ownership) is priced in ('attachments-as-channel (mooted by owning the schema outright)'). The wrap design also structurally forecloses Sentry's offline transport, since it passes a custom Transport that replaces the transport stack entirely — a mitigation the report leaves unstated. A one-clause fix to an otherwise complete comparison: minor, not major.

#### COV-4 — Three of the design's compensations rest on backend behaviours nobody has been asked for; only one (absence-based detection) is carried as a declared contingency. Backend deduplication is asserted as fact in two places and appears in neither the assumptions list nor the intake register — an undeclared assumption under D-0004, which the design register flagged and no amendment closed.

- **Severity (post-refutation)**: major  ·  **Lane**: coverage
- **Evidence**: report.md:697 asserts it as a property of the backend: "Identity is what makes at-least-once delivery deduplicable at the backend (a POST that lands but loses its 2xx is re-sent; **the backend upserts on `(deviceId, pageIncarnationId, bundleId)`**)". report.md:751 asserts a second, different dedup: "**the backend dedups by `(deviceId, trigger time-bucket, reason)`** for reporting purposes, which costs nothing client-side". The report's own "Still assumed, and each with its consequence if wrong" list (report.md:813-824) carries ten bullets — REST body sizes, appBuild readability, deviceId permissibility, gzip decoding, xstate machine version, outbound volume, CSP, backend response headers, incumbent handlers, workers — and none about dedup or upsert capability. Intake item c (intake/2026-08-22-0160-recorder-facts.md:66-85) asks four numbered questions — origin, size/gzip, auth, infrastructure sharing — none about the endpoint's storage or key semantics. The design register named it and the report did not act: design-fmea-enumeration.md:1083-1084 — "- **Owner**: `partial:Q10 ... UNOWNED for the fact itself`" / "the report's own 'Assumptions in place of facts' list contains nothing about backend dedup capability while listing nine other assumptions. The dependency is load-bearing and undeclared." By contrast the report does declare the parallel backend dependency at report.md:790: "Absence-based detection at the backend is the sole compensation, it is backend work keyed on this report's identity fields, and if nobody builds it those deaths stay dark."
- **Why it matters**: Three client-side rulings were chosen *because* the backend would dedup: at-least-once retry with bounded-then-drop (question 10), the refusal of cross-tab coordination with N near-duplicate bundles accepted (question 16), and the refusal of durable parking. If the endpoint is append-only — the common shape for a log intake, and it is a service the team is still building — every ambiguous delivery outcome stores a duplicate and every two-tab device stores near-duplicates, and the fix is a backend schema change made after the vendored contract froze. CLAUDE.md is explicit that a question only the user can answer goes to `intake/`, "not into a report as a silent assumption".
- **Proposed fix**: Add one bullet to the "Still assumed" list — "The report endpoint deduplicates on `(deviceId, pageIncarnationId, bundleId)` rather than appending: at-least-once retry, the two-tab duplicate acceptance, and the durable-parking refusal all rest on it; if it appends, incident counts inflate and the fix is a backend storage change" — and add the corresponding sub-question to a new dated intake item (item c is answered and D-0009 keeps resolved files in place).
- **Refuter**: Confirmed on every quote. report.md:697 asserts 'the backend upserts on `(deviceId, pageIncarnationId, bundleId)`' and report.md:751 asserts 'the backend dedups by `(deviceId, trigger time-bucket, reason)` for reporting purposes, which costs nothing client-side' — both in the present indicative, as properties of a service intake item c's own answer describes as one the team is still building ('It will be a service with an endpoint in the cloud to accept our payloads', intake:85). I read the full 'Still assumed' list (report.md:813-824): ten bullets — REST body sizes, appBuild readability, deviceId permissibility, gzip decoding, xstate version, outbound volume, CSP, backend response headers, incumbent handlers, workers — and none names dedup or upsert capability. Intake item c (intake:66-85) asks origin, size/gzip, auth, infrastructure sharing, and nothing about storage or key semantics. The register quote is exact: design-fmea-enumeration.md:1083-1084, owner `partial:Q10 ... UNOWNED for the fact itself`, rationale 'the report's own "Assumptions in place of facts" list contains nothing about backend dedup capability while listing nine other assumptions. The dependency is load-bearing and undeclared.' It appears in neither the 28-amendment table nor 'What was not acted on'. Partial mitigation only: report.md:711 frames at-least-once as told to the endpoint's owners ('the report says so to the endpoint's owners (intake item c) with the dedup key from question 9') — but item c carries no such clause, so the delegation lands nowhere, and the contrast with the properly declared parallel dependency at report.md:790 ('Absence-based detection at the backend is the sole compensation ... if nobody builds it those deaths stay dark') shows the report knows the shape and did not apply it here. A fact asserted that the intake never supplied, load-bearing on three client-side rulings: major stands.

#### COV-5 — The report states that intake item c "gains the question" about a route and parser for the last-gasp loss marker; the intake register contains no such question, so the ask that the entire compensation for refusing durable parking depends on will never be put to anyone.

- **Severity (post-refutation)**: major  ·  **Lane**: coverage
- **Evidence**: report.md:699: "**The marker is part of the vendored contract, not an undocumented second format** (added 2026-08-23): it gets its own schema and route in the OpenAPI document alongside the bundle, and **intake item c gains the question** — because a beacon's response is unobservable, an endpoint with no route or parser for it discards the entire compensation for refusing durable parking, and the client can never learn that happened." The register records the same intent: design-fmea-enumeration.md:59 — "The marker joins the vendored contract with its own route and `formatVersion`; **added to intake item c**". But intake/2026-08-22-0160-recorder-facts.md:66-85 (item c) carries only its four original numbered sub-questions plus the user's answers, with no addition; and the file's header note (lines 6-9) names only the two additions that were actually made: "**Two items remain open**: **m** and **n** below, added 2026-08-23 from the failure-mode enumeration over the recommended design." A grep of the whole intake directory for "beacon"/"loss marker"/"parser" returns only the two pre-existing occurrences at lines 71 and 116 (the original CORS and keepalive clauses) — nothing about a marker route.
- **Why it matters**: The report refuses durable parking (report.md:713) and names the loss marker as the compensation: "Until both hold, the loss marker (question 9) is the compensation: losses are reported, not silent." The report's own text says a beacon's response is unobservable, so if the endpoint has no route for the marker the client can never learn the compensation is dead. Routing that to intake and then not adding it means the gap ships invisible — the exact failure the report cites as its reason for raising it. It also makes a checkable claim about the intake register false, which matters because the intake register is what the next session reads to know what is still owed.
- **Proposed fix**: Either add the question to the intake register as a new dated item (item c is answered; D-0009 keeps resolved files in place) — "Will the report endpoint expose a second route and parser for the ≤64 KiB `text/plain` loss marker, and what does it do with one it cannot parse?" — or change report.md:699 from "intake item c gains the question" to a statement that the question is raised as intake item o/open, and add the marker route to the "Still assumed" list with its consequence.
- **Refuter**: Confirmed. report.md:699 states '**The marker is part of the vendored contract, not an undocumented second format** (added 2026-08-23) ... and **intake item c gains the question**'. I read the whole intake file: item c (intake:66-85) carries its four original numbered sub-questions and the user's four answers, with nothing added; the header note (intake:6-9) names only 'Two items remain open: **m** and **n** below, added 2026-08-23 from the failure-mode enumeration'; items m and n (intake:200-231) are CSP and outbound MQTT volume. A grep of the entire intake directory for beacon/marker/parser/formatVersion returns only intake:71 and intake:116, both pre-existing CORS/keepalive clauses. The register records the same unexecuted intent at design-fmea-enumeration.md:59 ('added to intake item c') and the underlying mode at :1070-1076 is UNOWNED. So the report makes a checkably false statement about the state of the intake register, and the ask on which its own stated compensation depends — report.md:713, 'Until both hold, the loss marker (question 9) is the compensation: losses are reported, not silent' — is never put to the backend owners, in a case where the report itself says the client can never detect the failure. Partial mitigation: the design requirement is at least stated in the report's prose (own schema and route in the OpenAPI document, own `formatVersion`), so it is not wholly invisible, and the fix is one line in either file. Not verdict-changing, but it is exactly the 'reads as closed when it is not' failure CLAUDE.md's intake convention exists to prevent: major.

#### COV-3 — The report says four of the twelve intake answers settled less than their question asked; D-0042 (the accepted ledger entry minted from this exact round) and the intake register both say six, and the report's own "Still assumed" list carries more than four.

- **Severity (post-refutation)**: minor  ·  **Lane**: coverage
- **Evidence**: report.md:796: "**Four of the twelve** answers settled less than their question asked, and each is carried below rather than read as closed." DECISIONS.md:325 (D-0042, Why): "Twelve items came back answered; **six settled less than their question asked**, and four of those were initially *read as closed* — a described scrubbing *mechanism* read as a ruled disclosure *policy*, an *available* device identifier read as a *permitted* one, a *defined* build-id format read as a *runtime-readable* one, and an endpoint's size answer read as covering compression." intake/2026-08-22-0160-recorder-facts.md:11: "**Six answers did not fully close their questions**, and the report carries each as a declared assumption rather than inferring the rest" — then lists b, a, c, e, i, h. The report's own list at report.md:813-824 carries under-answered halves of a, b (via risk 2), c, d, e, g, h, i and k. The report appears to have transcribed D-0042's "four of those were initially read as closed" as the count of under-answered items.
- **Why it matters**: The count is the reader's checksum on the section whose whole job is honesty about the fact base — a reader verifying "is each one carried?" looks for four and stops. It also puts the report in contradiction with an accepted ledger entry whose entire rationale is derived from the number six, and with the intake register that is the authority on what is still open.
- **Proposed fix**: Change "Four of the twelve" to "Six of the twelve" at report.md:796, matching D-0042 and the intake register's header note, and optionally add the distinction D-0042 draws: four of the six were initially read as closed and were caught by the verification pass.
- **Refuter**: Confirmed against all three authorities. report.md:796: 'Four of the twelve answers settled less than their question asked, and each is carried below rather than read as closed.' DECISIONS.md:325 (D-0042, Why): 'Twelve items came back answered; **six settled less than their question asked**, and four of those were initially read as closed'. intake/2026-08-22-0160-recorder-facts.md:11-27: '**Six answers did not fully close their questions**' followed by six named items — b, a, c, e, i, h. The report's own material bears out six rather than four: the facts table (report.md:807-812) bolds under-answered halves for b, e and i, the assumptions list (report.md:813-824) carries a, b (via risk 2), c, d, e, g, h, i and k, and no reading of the table yields exactly four. The likeliest cause is the transcription the finding proposes — D-0042's 'four of those were initially read as closed' read as the total. A reader's checksum on the honesty section, in contradiction with an accepted ledger entry derived from the number six: real, and correctly filed as minor.

#### ASM-1 — The report undercounts the partially-answered intake items: it says four of the twelve answers settled less than their question asked, where the intake register — the authority — says six, and the report's own risk 2 flatly asserts "Items a–l are answered" with "the endpoint design ... settled" while its own Still-assumed list holds an open gzip question and an undecided auth arrangement from that same item.

- **Severity (post-refutation)**: minor  ·  **Lane**: assumptions
- **Evidence**: /home/nn/Projects/app-tooling-research/tracks/0160-flight-recorder/report.md:796 — "Four of the twelve answers settled less than their question asked, and each is carried below rather than read as closed." Contradicted by /home/nn/Projects/app-tooling-research/intake/2026-08-22-0160-recorder-facts.md:11 — "**Six answers did not fully close their questions**, and the report carries each as a declared assumption rather than inferring the rest" (the six enumerated at intake lines 14-27 are b, a, c, e, i, h). Compounding, /home/nn/Projects/app-tooling-research/tracks/0160-flight-recorder/report.md:787 — "Items a–l are answered (see Facts, below): the endpoint design and the config seeds are settled", against the report's own /home/nn/Projects/app-tooling-research/tracks/0160-flight-recorder/report.md:818 — "**The report endpoint will decode gzipped request bodies** — item c answered the size half of its compression question and not the encoding half".
- **Why it matters**: The "Facts, and what remains assumed" section is the report's own claim to carry every gap, and it is the paragraph a decision-maker reads to size the open surface. Two of the six partials (item a's REST half, item h's feasibility half) appear only in the Still-assumed bullets and are invisible in both the count and the fact table, so the summary sentence and the table disagree with each other and with the intake register.
- **Proposed fix**: Change "Four of the twelve" to "Six of the twelve" and mark the a and h rows of the fact table with the same bolded "not answered" clause the b/e/i rows already carry; soften risk 2's "Items a–l are answered ... the endpoint design ... settled" to "answered, six of them only in part — the endpoint's gzip and auth halves included".
- **Refuter**: The numeral is genuinely wrong. report.md:796 reads "Four of the twelve answers settled less than their question asked"; the intake header (intake/2026-08-22-0160-recorder-facts.md:11) says "**Six answers did not fully close their questions**" and — decisively — the accepted ledger says the same: DECISIONS.md D-0042 states "Twelve items came back answered; **six settled less than their question asked**, and four of those were initially read as closed." So "four" is almost certainly a stale echo of D-0042's *misread* four (b, i, e, c), and it contradicts an accepted decision. But the rest of the finding is overstated, which is why this is not major. The sentence's substantive promise — "each is carried below rather than read as closed" — is kept for all six: a at report.md:815, e at 816, i at 817, c at 818, h at 822, and b in the fact table (802, "**The policy is still unruled**") plus risk 2. The claim that a and h are "invisible in both the count and the fact table" is half wrong: both have explicit Still-assumed bullets in the same section, and item a's MQTT-only scope is also stated at report.md:71 ("Item a answered for MQTT only"). The "compounding" charge against risk 2 does not hold either: report.md:787 says "Items a–l are answered (see Facts, below)" — literally true, with a pointer to the section that carries the partials — and it self-limits in the same sentence ("still sized by analogy for the other three buckets"), while the fact table row at 803 records "auth undecided (c)" and 818 records the gzip half. One-word fix; no verdict moves.

#### ASM-2 — The report asserts that intake item c now carries a question about the loss-marker beacon route; item c carries no such question, and the intake file lists only m and n as open — so a design dependency the report calls load-bearing is recorded in no register anybody will answer.

- **Severity (post-refutation)**: minor  ·  **Lane**: assumptions
- **Evidence**: /home/nn/Projects/app-tooling-research/tracks/0160-flight-recorder/report.md:699 — "it gets its own schema and route in the OpenAPI document alongside the bundle, and intake item c gains the question — because a beacon's response is unobservable, an endpoint with no route or parser for it discards the entire compensation for refusing durable parking, and the client can never learn that happened." Item c's four numbered sub-questions (/home/nn/Projects/app-tooling-research/intake/2026-08-22-0160-recorder-facts.md:70-79) ask only same-origin/cross-origin, maximum size + gzip, auth, and shared infrastructure; the header note (same file, lines 8-9) says "**Two items remain open**: **m** and **n**". Same pattern at report.md:69 ("so intake item c's size question must be asked and answered in wire bytes") and report.md:711 ("the report says so to the endpoint's owners (intake item c)").
- **Why it matters**: CLAUDE.md requires questions only the user (here, the endpoint's owners) can answer to land in intake as a dated file, not as a report assertion. The report refuses durable parking and names the loss marker as the sole compensation; if the endpoint has no route or parser for it, the compensation silently does not exist and, by the report's own argument, the client can never learn that. Saying an intake item "gains the question" makes it look tracked when it is not.
- **Proposed fix**: Raise the three unrecorded endpoint asks (a beacon/loss-marker route and parser, a wire-byte ceiling rather than a ring-byte one, at-least-once dedup on `(deviceId, pageIncarnationId, bundleId)`) as a new dated intake file per D-0042, cite that file instead of "item c gains the question", and add one Still-assumed bullet: "the endpoint will expose a route that accepts the loss marker", with the consequence already written in Q9.
- **Refuter**: Partly real, mostly overstated. Verified: report.md:699 says the loss marker "gets its own schema and route in the OpenAPI document alongside the bundle, and intake item c gains the question", and intake item c (lines 66-85) has four numbered sub-questions — origin, size+gzip, auth, shared infrastructure — none about a beacon route, with the header (lines 8-9) declaring only m and n open. So the intake file does not carry the question the report says it gains. That is a checkable bookkeeping mismatch. But the two supporting citations collapse: report.md:69 ("intake item c's size question must be asked and answered in wire bytes") refers to an existing sub-question whose answer was "negotiable", i.e. still to be negotiated — not an invented one; and report.md:711 ("the report says so to the endpoint's owners (intake item c)") cites item c to identify the endpoint's owners, which is exactly what the intake header line 4 makes it ("plus whoever owns the report backend (c, h)"), not a claim that item c contains a dedup question. The severity claim also overreads: D-0040 ruling (2) makes the payload schema 0160's own vendored contract, so specifying the marker's route in that OpenAPI document is this track's deliverable that the endpoint implements — and the report states the consequence of an endpoint without it in the same sentence, so it is declared, not silent. Worth fixing by adding the ask to the register or rephrasing "gains" as "must gain".

#### ASM-6 — The report places its proposed abort counters on "the existing `stats` surface" of 0060, a surface no accepted 0060 artifact carries — in the same sentence-set that correctly says the D-0018 counter "remains ratified language with no design".

- **Severity (post-refutation)**: minor  ·  **Lane**: assumptions
- **Evidence**: /home/nn/Projects/app-tooling-research/tracks/0160-flight-recorder/report.md:102 — "visibility via a \"boundary stats counter\" that remains ratified language with no design (DECISIONS.md:147-152)" and then "the stats counter as three monotonic integers on the existing `stats` surface (`aborted`, `timedOut`, `settled`), read at bundle-assembly time by the transport adapter". The accepted 0060 interface has no such member: /home/nn/Projects/app-tooling-research/tracks/0060-transport-abstraction/spikes/boundary-wiring/design.md:220-261 declares `TransportBoundary` with `actor`, `fetcher`, `quarantine`, `start()`, `reconnect()`, `dispose()`; the only counter-shaped block is design.md:300 — "readonly depths: { readonly delivery: number; readonly publish: number; readonly quarantine: number };". `stats` appears nowhere else in that file, and "stats" appears nowhere in /home/nn/Projects/app-tooling-research/tracks/0060-transport-abstraction/report.md.
- **Why it matters**: The proposal framing around it is correct ("Whether to build it is 0060's call"), but the word "existing" asserts a state of another track's interface that is false, and it hides the real cost of the proposal — 0060 would have to add a surface, not a field. A 0060 owner reading this would look for a `stats` member and not find one.
- **Proposed fix**: Say "a new `stats` surface on `TransportBoundary` — or three fields on the existing `BoundarySnapshot.depths` block — which 0060 would have to add", keeping the existing "whether to build it is 0060's call" sentence.
- **Refuter**: Verified as written, and it is a small factual error about another track's interface. report.md:102 proposes "the stats counter as three monotonic integers on the existing `stats` surface (`aborted`, `timedOut`, `settled`)". I read the accepted 0060 interface: design.md:220-261 declares `TransportBoundary` with `actor`, `subscribe`, `publish`, `fetcher`, `quarantine`, `start()`, `reconnect()`, `dispose()`, and the only counter-shaped block is `BoundarySnapshot.depths` at design.md:300. A grep of the whole 0060 track for "stats" returns exactly one hit — design.md:587, the D-0018 gate note "a boundary stats counter carries visibility" — i.e. ratified language, not a surface. (The nearest real `stats` member is 0070's `IngressFeed.stats: IngressStats`, spike design.md:299, which is MQTT ingress drop accounting and cannot host REST abort counters.) Downgraded from major because the surrounding framing is correct and non-misleading: the same bullet calls the counter "ratified language with no design", says 0160 supplies it "as a proposal to 0060's owners", and closes "Whether to build it is 0060's call". Only the word "existing" misstates the host surface's cost.

#### ASM-8 — The report states that intake items a–l are recorded in facts/app-profile.md; item l's answer is not there, so the only durable record of the fact two of question 10's verdicts rest on lives in the intake file alone.

- **Severity (post-refutation)**: minor  ·  **Lane**: assumptions
- **Evidence**: /home/nn/Projects/app-tooling-research/tracks/0160-flight-recorder/report.md:796 — "Intake items a–l were answered on 2026-08-23 and are recorded in facts/app-profile.md". /home/nn/Projects/app-tooling-research/facts/app-profile.md cites 0160 intake items a (line 66), d (321), e (337), f (331), g (358), b (362), i (345), j (351), k (373), c (381), and f+h (296) — there is no entry for item l ("Nobody has measured", intake line 195), and no line in the profile records tab-crash, reload, or backend-outage rates. The report leans on it twice: report.md:711 — "item l came back \"nobody has measured\", so the retry horizon is a judgment defaulting safe" and report.md:713 — "it cannot be quantified — item l came back \"nobody has measured\"".
- **Why it matters**: D-0004 makes facts/app-profile.md the place reports cite for app facts; a fact that never lands there is invisible to the next track that needs it (0150's delivery and recovery design has the same question). The claim as written also overstates what the profile carries.
- **Proposed fix**: Either add the item-l entry to facts/app-profile.md (Environment: "Delivery-interruption rate: not measured (2026-08-23, user — intake 2026-08-22-0160 item l)"), or narrow the report's sentence to "items a–k are recorded in facts/app-profile.md; item l's answer stands in the intake file only".
- **Refuter**: Verified. report.md:796 asserts "Intake items a–l were answered on 2026-08-23 and are recorded in facts/app-profile.md". A grep of facts/app-profile.md for "0160" returns citations to items a (66), f+h (295), d (321), f (331), e (337), i (345), j (350), g (358), b (361), k (373), c (381) — eleven of twelve, no item l — and a grep for "interrupt", "nobody has measured" and "outage" turns up nothing recording tab-crash, reload, or backend-outage rates. The report leans on that answer twice (report.md:711 and :713), so the durable home for it is the intake file alone. Minor rather than major: the missing fact is a non-measurement, the report declares it correctly in the fact table ("Nobody has measured interruption rates (l) | The pre-declared acceptable answer"), and the intake header itself makes the same overstatement ("All twelve are recorded in facts/app-profile.md"), so the report inherited the error rather than fabricating it.

#### ARI-1 — Question 2's "~1.5 MB" analogy term does not reproduce from the operands stated in the same sentence — the correct product is ~0.95 MB — and the ~2.5 MB / ~6.5 MB totals inherit the error.

- **Severity (post-refutation)**: minor  ·  **Lane**: arithmetic
- **Evidence**: tracks/0160-flight-recorder/report.md:73 — "so sizing them by analogy at roughly 10 KB per HTTP record, 4 KB per transition and 1 KB per log line adds about **1.5 MB**, for ~2.5 MB typical and ~6.5 MB worst". Re-derivation with the depths the same paragraph states ("the day-one depths are 100 MQTT messages, 25 HTTP exchanges, 100 xstate transitions, and 50 log lines", report.md:71) and the report's own two-record HTTP model (report.md:611 "25 exchanges = 50 records under the two-record model (request + outcome)"): HTTP 50 records x 10 KB = 500 KB; xstate 100 transitions x 4 KB = 400 KB; log 50 x 1 KB = 50 KB. Sum = 950 KB = ~0.95 MB, not 1.5 MB (a 58% overstatement). Totals therefore become 1 + 0.95 = ~1.95 MB typical and 5 + 0.95 = ~5.95 MB worst, not ~2.5 / ~6.5. No alternative reading recovers 1.5 MB: at 25 HTTP records the sum is 700 KB; even crediting the xstate row's own note that a transition yields 2 records (report.md:613 "100 records ≈ FEWER than 100 transitions: a transition emits a microstep event plus its external event") gives 500 + 800 + 50 = 1,350 KB. The report's own cross-check uses the 950 KB operands and reproduces exactly: report.md:597-599 "at the analogy record sizes in Q2, http/xstate/log expect to hold well under a quarter of theirs" — 500/4096 KiB = 12%, 400/2048 KiB = 19%, 50/1024 KiB = 5%. The same unsupported 1.5 MB is carried into facts/app-profile.md:325 "sizing them by analogy adds roughly 1.5 MB, for ~2.5 MB typical and ~6.5 MB worst", there with no operands printed at all.
- **Why it matters**: Question 2 is the report's byte-arithmetic section and the place a reader checks the envelope sizing; a 58% error in the only non-MQTT term is the kind of number that gets lifted into a config decision. The verdict ("memory is not the binding constraint at these depths") survives, but the figure that supports it is wrong and has already propagated into the facts file.
- **Proposed fix**: Replace "adds about **1.5 MB**, for ~2.5 MB typical and ~6.5 MB worst" with "adds about **0.95 MB**, for ~2 MB typical and ~6 MB worst", and print the counting rule beside it (50 HTTP records x 10 KB + 100 transitions x 4 KB + 50 log lines x 1 KB). Correct facts/app-profile.md:325 to match.
- **Refuter**: Partly verified, materially overstated. report.md:73 does read "sizing them by analogy at roughly 10 KB per HTTP record, 4 KB per transition and 1 KB per log line adds about **1.5 MB**, for ~2.5 MB typical and ~6.5 MB worst", and the depths in the same paragraph (report.md:71 "100 MQTT messages, 25 HTTP exchanges, 100 xstate transitions, and 50 log lines") give 250+400+50 = 700 KB, so the product does not reproduce from the operands as written. But the finding's central assertion — "No alternative reading recovers 1.5 MB" and "the correct product is ~0.95 MB ... a 58% overstatement" — does not survive: applying BOTH of the report's own record-conversion notes (report.md:611 "25 exchanges = 50 records under the two-record model" AND report.md:613 "100 records ≈ FEWER than 100 transitions: a transition emits a microstep event plus its external event") yields 500+800+50 = 1,350 KB, i.e. ~1.35 MB, which "about 1.5 MB" rounds to conservatively. The finding itself computes this figure and then discards it while selectively applying only the HTTP conversion. The figure is explicitly labelled analogy/estimate (facts/app-profile.md:326 "an estimate, not a fact"), the error direction is conservative, and no verdict moves (the "two tabs at worst-case payloads is the first figure that would press" the 10 MB floor conclusion holds at 700 KB, 950 KB and 1,350 KB alike). Real enough to fix in place — the counting rule should be printed beside the sentence — but not a 58% error and not major.

#### ARI-3 — Risk 6's "under 2%" of device RAM is computed from ring bytes only; adding the memory the report's own delivery and seal designs hold at the same time pushes the figure above 2% under every reading.

- **Severity (post-refutation)**: major  ·  **Lane**: arithmetic
- **Evidence**: tracks/0160-flight-recorder/report.md:791 — "at the worked configuration this track contributes ~40 MiB of serialized bytes per device, nearer 80 MiB of heap by question 2's UTF-16 caveat, against 6–16 GB of contended RAM, which is under 2%". That reproduces for rings alone: 2 tabs x 20 MiB = 40 MiB metered, x2 for UTF-16 = 80 MiB, 80 / (6 x 1024 MiB) = 1.30%. But report.md:701 budgets a seal-time peak of "roughly envelope + largest-bucket rather than 2–3× envelope — in metered serialized bytes, so the heap figure the spike must actually check can run to about twice that": 20 + 12 = 32 MiB per tab, x2 tabs = 64 MiB metered, x2 = 128 MiB heap, 128/6144 = 2.08% — already over. And report.md:705 keeps "one bundle in flight at a time plus **at most one pending**", each sized at report.md:711 as "the pinned bundle is 10–50 MB of RAM on a RAM-disk device": 2 tabs x 2 slots x ~19 MiB of ring bytes = 76 MiB metered on top of the 40 MiB of rings = 116 MiB metered, ~232 MiB heap, 232/6144 = 3.78%. Using the report's own "10–50 MB" per pinned bundle the figure is larger still. The 16 GB end stays comfortable (0.5%–0.9%); the 6 GB end does not.
- **Why it matters**: This percentage is the whole disposition of the D-0043 residual risk — it is what makes the sum "comfortable" and closes the memory-contention item against 0150. The number omits the two largest transient holders the same report designs, and the ceiling it clears is 2% at the low end of the stated RAM range.
- **Proposed fix**: State the per-device budget as rings + seal peak + delivery slots — e.g. "~40 MiB of rings plus up to ~76 MiB of sealed-but-undelivered bundles per device, nearer 230 MiB of heap, about 3.8% of a 6 GB device and 1.4% of a 16 GB one" — and let the residual stand on that figure, or state explicitly that the 2% covers rings only and name the transient separately.
- **Refuter**: Verified on its core, though its universal is false. report.md:791 does say "at the worked configuration this track contributes ~40 MiB of serialized bytes per device, nearer 80 MiB of heap by question 2's UTF-16 caveat, against 6-16 GB of contended RAM, which is under 2%", and that reproduces for rings only (2 x 20 MiB envelope, x2 for UTF-16, 80/6144 = 1.3%). The same report sizes the omitted holders itself: report.md:705 keeps "one bundle in flight at a time plus **at most one pending**" and report.md:711 prices one as "the pinned bundle is 10-50 MB of RAM on a RAM-disk device", with report.md:707 noting "by then the rings have evicted its contents, so the bundle is their only copy" — i.e. held bundles are additive, not string-shared with the rings. Decisively, the enumeration item this risk answers already named them: design-fmea-enumeration.md:2022 says 0150's store competes with "the recorder's ... rings per tab plus up to two held bundles", and the report's disposition drops the held bundles from the arithmetic while claiming to close the residual with it. So the number understates the track's own bounded device footprint by roughly 2-3x, in a risk whose whole disposition is "the residual is arithmetic rather than analysis, and it is comfortable". Where the finding overreaches: "pushes the figure above 2% under every reading" is false — one tab sealing while the other is quiescent gives 52 MiB metered / ~104 MiB heap = 1.7% — and "comfortable" survives at every reading (worst case ~3.8% of a 6 GB device), so no decision changes. Correction owed, acceptance not held up.

#### ARI-5 — The worked table's only I3 justification prints Sigma maxBytes and drops the two other terms I3's formula requires; the residual that decides whether the table validates is never computed, and two of the formula's terms plus the quarantine cap have neither a value nor a config field.

- **Severity (post-refutation)**: minor  ·  **Lane**: arithmetic
- **Evidence**: tracks/0160-flight-recorder/report.md:605 annotates the table with "maxBytes: 12 * MiB,               // Σ over all rows validated ≤ envelopeBytes (I3)", but I3 at report.md:131-132 is "Σ(maxBytes + PER_RECORD_OVERHEAD × maxCount) + headerAllowance ≤ config.envelopeBytes". Re-derived: Sigma maxBytes = 12+4+2+1 = 19 MiB against envelopeBytes 20 MiB (report.md:583), so the whole residual is 1 MiB = 1,048,576 B; Sigma maxCount = 1200+50+100+50 = 1,400, so the table validates only if PER_RECORD_OVERHEAD + (headerAllowance/1400) <= 749 B per record. At the report's own estimate (report.md:68, "tens of bytes") that is roughly 10x of margin — the table does leave room — but the report never prints either figure. `headerAllowance` appears only inside the formula (report.md:131, 193) with no value and no field in `RecorderConfig` (report.md:170-244), and the quarantine ride-along is "byte-capped separately" (report.md:134, 468-469) with no number and no config field either, while report.md:699 relies on those same unbounded-in-text terms to call 28–34 MB "a **floor** for the uncompressed worst case, since the ring meter excludes header, marker, and quarantine-ride-along bytes".
- **Why it matters**: The consequence of failing this check is stated as fleet-wide inertness ("a config whose Σ maxBytes exceeds the envelope is a **table-level** validation error ... the recorder comes up disabled-loud", report.md:67), and the worked table sits 5% away from it. A reader tuning the table cannot tell how much of the 1 MiB is already spoken for, and the 50 MB wire ask rests on terms no number bounds.
- **Proposed fix**: Print the residual beside the table ("19 MiB of ring bytes leaves 1 MiB for 1,400 records of PER_RECORD_OVERHEAD plus headerAllowance — <= 749 B per record"), give `headerAllowance` and the quarantine byte cap either default values or a home in `RecorderConfig`, and add both to the spike's measurement list alongside PER_RECORD_OVERHEAD (report.md:833).
- **Refuter**: Mostly refuted; one grain survives. The headline — "the residual that decides whether the table validates is never computed" — is technically true but inert: the report prints every operand needed (report.md:699 "the worked table's ~19 MiB of ring bytes"; envelopeBytes 20 * MiB at report.md:583; PER_RECORD_OVERHEAD stated as "tens of bytes" at report.md:68 with "The constant's measured value is a spike deliverable"), so a reader gets ~1 MiB of slack against ~tens of KB of overhead — a ~10x margin the finding itself concedes ("the table does leave room"). The quarantine-cap complaint is refuted outright: the missing number is precisely why report.md:699 labels 28-34 MB "a **floor** for the uncompressed worst case, since the ring meter excludes header, marker, and quarantine-ride-along bytes" and asks for 50 MB rather than 40 MB. What survives is narrow and real: `headerAllowance` occurs only inside the formula (report.md:68, 131, 193), has no value, no field in `RecorderConfig` (report.md:170-244, which does carry `envelopeBytes`), and no entry in the spike's measurement list (report.md:833 names PER_RECORD_OVERHEAD and the wire ratio only) — the exact hidden-constant defect the report's own `envelopeBytes` comment says it was added to fix ("I3 previously named a bound the config surface had no field to express"). Minor, fixable in place.

#### ARI-6 — The rubric's Runtime overhead row compares four numbers computed under three different counting rules, and Faro's figure appears nowhere else in the report or the plan and carries no source.

- **Severity (post-refutation)**: minor  ·  **Lane**: arithmetic
- **Evidence**: tracks/0160-flight-recorder/report.md:765 — "| Runtime overhead (high) | strong — ~6.5 KB gzip of deps plus the owned kit ... | adequate — ≤33 KB gzip error-only (CI budget) ... | adequate — 15.1 KB gzip, moot given the ceiling | adequate — 34.4 KB gzip incl. web-vitals/ua-parser |". The build's 6.5 KB reproduces (2,513 B + 4,027 B = 6,540 B, report.md:22-23) and is deps-only; Sentry's 33 KB is an error-only subset the report explicitly contrasts with the whole-package figure — "Bundlephobia's 145 KB gzip figure measures importing the entire package including replay/feedback and must not be quoted as the error-only cost" (report.md:39); Bugsnag's 15.1 KB is the whole package (15,128 B, report.md:45); Faro's 34.4 KB is a whole-package figure with the extras named, and it occurs nowhere in the Faro survey section (report.md:47-49), nowhere else in report.md, and nowhere in research-plan.md (which lists Faro with no size at all, research-plan.md:557-558). The row therefore sets a subset figure against two whole-package figures. The report names one asymmetry in this row ("Runtime overhead scores the build on its dependency bytes with the owned kit unquantified", report.md:772) but not this one.
- **Why it matters**: Runtime overhead is a high-weight rubric criterion. A reader comparing 33 KB to 34.4 KB is comparing Sentry's error-only import against Faro's full SDK plus web-vitals and ua-parser, and the one number that cannot be traced to any source in the track is the one that makes the two look equal.
- **Proposed fix**: Give the Faro figure a source in the Survey section like every other candidate, and label each cell with what it measures (deps-only / error-only subset / whole package), or add the counting-rule asymmetry to the evidence-asymmetry note at report.md:772.
- **Refuter**: Half refuted, half verified. The counting-rule complaint is weak: each cell at report.md:765 labels its own basis ("~6.5 KB gzip of deps", "<=33 KB gzip error-only (CI budget)", "15.1 KB gzip", "34.4 KB gzip incl. web-vitals/ua-parser"), and report.md:39 explicitly warns that Bundlephobia's whole-package 145 KB "must not be quoted as the error-only cost" — so the report is transparent about what Sentry's figure measures rather than hiding it. What I did verify is the sourcing gap: grep shows 34.4 occurs exactly once in the whole track, at report.md:765, and the Faro survey section (report.md:47-49) gives no size at all while safe-stable-stringify (Bundlephobia API link), Sentry (.size-limit.js link) and Bugsnag (15,128 B) each carry a primary source. One number in a high-weight rubric row that a reader cannot trace to anything. Consequence is small — Faro is rejected on "not a recorder", not on bytes (report.md:782) — so minor, as filed.

#### DEV-1 — The Bugsnag source note claims Bugsnag is the only branch-pinned candidate, but the Faro survey section is sourced entirely from the `main` branch while asserting a tag-level read of "the 2.10.0 tree" — so a same-class deviation is both undisclosed for Faro and actively denied by the note.

- **Severity (post-refutation)**: minor  ·  **Lane**: deviations
- **Evidence**: report.md:45 — "(Source note: the Bugsnag files were read on the repository's default branch, which carried 8.9.0 at survey time rather than the published 8.10.0; the cited code is identical wherever both were checked, but the citations are branch-pinned, not tag-pinned, **unlike every other candidate here**.)"  CONTRADICTED BY report.md:49 — "an exhaustive grep of the **2.10.0 tree** finds no ring, breadcrumb trail, or replay store in any package" — whose only supporting source is report.md:934 "https://api.github.com/repos/grafana/faro-web-sdk/git/trees/**main**?recursive=1 — accessed 2026-08-23", with every Faro file citation likewise branch-pinned (report.md:922-930, e.g. ".../faro-web-sdk/**main**/packages/core/src/transports/batchExecutor.ts"). Report.md:49 also states "2.10.0 published 2026-08-17; repo pushed the day of this survey", i.e. `main` was up to six days ahead of the tag. The two recommended micro-deps are in the same position: report.md:995 ".../csnover/TraceKit/**master**/tracekit.js" for "tracekit 0.4.9" (report.md:23) and report.md:980 ".../mnemonist/**master**/circular-buffer.js" for "mnemonist 0.40.4" (report.md:24). Only Sentry (10.70.0), safe-stable-stringify (v2.5.0), fast-redact (v3.5.0) and xstate (5.32.5) are actually tag-pinned.
- **Why it matters**: This is the one sentence in the report whose job is to disclose a verification deviation, and as written it is false. It tells a reader that every non-Bugsnag candidate fact was read at its evaluated tag, when Faro's entire "confirmed in source" section — including the `ItemBuffer` unboundedness and drop-oriented offline behaviour that produce its "weak" Boundedness score — is branch-read and attributed to a version the citation cannot establish. The lens's own question ("is anything the report relies on actually unverifiable and not marked so?") is answered wrongly by the report itself.
- **Proposed fix**: Replace "unlike every other candidate here" with an accurate list, e.g. "as are the @grafana/faro-web-sdk citations (all read on `main`, which was ahead of the 2.10.0 tag at survey time) and the tracekit/mnemonist file citations; Sentry, safe-stable-stringify, fast-redact and xstate are tag-pinned." In report.md:49 change "an exhaustive grep of the 2.10.0 tree" to "an exhaustive grep of the `main` tree at 2026-08-23", or re-pin the Faro citations to the `v2.10.0` tag.
- **Refuter**: Textually verified. report.md:45 ends the Bugsnag block with "the citations are branch-pinned, not tag-pinned, unlike every other candidate here", while every @grafana/faro-web-sdk citation in the report is a `main`-branch read (report.md:923 ".../faro-web-sdk/main/packages/core/src/transports/batchExecutor.ts", report.md:922-930, tree API at report.md:934 ".../git/trees/main?recursive=1"), and report.md:49 attributes that read to "an exhaustive grep of the 2.10.0 tree". Sentry is genuinely tag-pinned (report.md:847-848, 10.70.0); tracekit/mnemonist are master-pinned (report.md:995, 980). So the clause is false as written. But the blocker framing does not survive: the deviation is not concealed — report.md:49 itself states "2.10.0 published 2026-08-17; repo pushed the day of this survey", and the Sources list prints the `main` URLs in full, so a reader can see the provenance; and nothing rides on it. Faro's verdict is an absence claim ("no ring, breadcrumb trail, or replay store") read on a tree that is *newer* than the tag, corroborated by architecture-level facts (ItemBuffer scope, batch-and-clear, drop-oriented offline) that six days of commits do not plausibly reverse. No score or verdict moves. This is a one-clause correction to a self-audit sentence, not a false claim that changes a verdict — real, but minor.

#### DEV-3 — The report's count of under-answered intake items contradicts both the intake file it cites and D-0042, which record six — and the report's own assumption list carries six.

- **Severity (post-refutation)**: major  ·  **Lane**: deviations
- **Evidence**: report.md:796 — "**Four of the twelve answers settled less than their question asked**, and each is carried below rather than read as closed."  CONTRADICTED BY intake/2026-08-22-0160-recorder-facts.md:11 — "**Six answers did not fully close their questions**, and the report carries each as a declared assumption rather than inferring the rest:" (items b, a, c, e, i, h enumerated at lines 14-27), and by DECISIONS.md:325 — "Twelve items came back answered; **six settled less than their question asked**, and four of those were initially read as closed". The report's own "Still assumed" bullets (report.md:815-824) plus its Facts table row (report.md:802, "**The policy is still unruled**") in fact cover all six, plus the unanswered half of item d.
- **Why it matters**: This is the report's headline deviation metric — the number a reader uses to check that the D-0004 assumption accounting is complete. It understates the gap by a third and reuses the figure D-0042 assigns to a different quantity ("four of those were **initially read as closed**"), which is precisely the confusion D-0042 was written to prevent.
- **Proposed fix**: Change "Four of the twelve" to "Six of the twelve" at report.md:796, and confirm the six named in the intake header (a, b, c, e, h, i) each appear below — adding item b's policy gap to the "Still assumed" bullets rather than leaving it only in the Facts table and Risk 2.
- **Refuter**: Confirmed against both authorities. report.md:796 reads "Four of the twelve answers settled less than their question asked". intake/2026-08-22-0160-recorder-facts.md:11 reads "Six answers did not fully close their questions" and enumerates b, a, c, e, i, h at lines 14-27. DECISIONS.md:325 reads "Twelve items came back answered; six settled less than their question asked, and four of those were initially read as closed" — so "four" is the ledger's count of a different quantity, exactly the conflation the finding names. The report's own accounting carries all six (a at report.md:815, e at 816, i at 817, c at 818, h at 822, and b at report.md:802 "The policy is still unruled" plus Risk 2 at 787), which is why this is not a blocker: no assumption is actually undeclared and no verdict moves. But the number is the metric a reader uses to check D-0004 completeness, it contradicts an accepted ledger entry and the intake file the same sentence cites, and it must be corrected. Major stands as filed.

#### DEV-7 — Within one bullet, the bare citation form `design.md:NNN-NNN` is used for two different files; `design.md:245-252` resolves to unrelated text in the 0060 spike design the bullet's four other bare citations point at.

- **Severity (post-refutation)**: minor  ·  **Lane**: deviations
- **Evidence**: report.md:89 — "(design.md:267-268 designs `'*'` as the wildcard tap; I12 at design.md:491 ...)", "(design.md:492, I13)", "(design.md:225-230)" — all 0060 — then "the recorder gets its pipeline-verdict visibility from the `IngressStats` counters instead (**six counters, design.md:245-252**)". tracks/0060-transport-abstraction/spikes/boundary-wiring/design.md:245-252 reads "opts?: { qos?: 0 | 1; retain?: boolean }, ): Promise<void>; ... /** The single REST ingress (0010's orval-mutator behaviour, instance-bound) ..." — no counters. The intended target is tracks/0070-state-concurrency/spikes/ingress-and-test-lane/design.md:245-252, "export interface IngressStats { readonly duplicate: number; ... readonly gaps: number; }", which does carry exactly six counters.
- **Why it matters**: Every other cross-file citation in the report either names its track ("0070 spike design.md:261", "0070 spike design.md:329-331") or is unambiguous from context. Here the reader following the bullet's established convention lands on 0060 text that contradicts the claim, which is the exact failure mode a file:line citation exists to prevent.
- **Proposed fix**: Write it as "0070 spike design.md:245-252" at report.md:89, matching the form already used for design.md:261 and design.md:329-331.
- **Refuter**: Verified in both files. In report.md:89 the bare `design.md` referent is established four times as the 0060 boundary-wiring spike (design.md:267-268 = "'*' is the 0050 wildcard tap" at that file's lines 267-268; design.md:491 = I12 handler isolation; design.md:492 = I13 no replay; design.md:225-230), and the same sentence switches to the explicit form "0070 spike design.md:261" for the inspect slot — then reverts to bare for "the `IngressStats` counters instead (six counters, design.md:245-252)". tracks/0060-transport-abstraction/spikes/boundary-wiring/design.md:245-252 is the publish signature and the REST-ingress doc comment ("opts?: { qos?: 0 | 1; retain?: boolean }"), no counters; tracks/0070-state-concurrency/spikes/ingress-and-test-lane/design.md:245-252 is exactly `IngressStats` with six readonly counters (duplicate, stale, masked, dispatched, unmatched, gaps). So the citation as written points a following reader at the wrong file, and the fix is the one-word track prefix already used elsewhere in the same sentence. Minor, as filed.

### Lens 2 — machinery (7 survived)

#### MACH-7 — Nothing rechecks a decision's load-bearing arithmetic when the world moves, so a repair declined on "N=0" grounds stays declined after N stops being 0 — and the defect it would have fixed is now present in this track's own plan.

- **Severity (post-refutation)**: minor  ·  **Lane**: machinery:stages
- **Evidence**: DECISIONS.md:285 (D-0037, 2026-08-17) — "Two arithmetic findings did more work than any argument: the research-plan template has **zero remaining authorings**, so changing it propagates into nothing". Two research plans have been authored since: `tracks/0150-message-inbox/research-plan.md` (commit 54cc20f, after D-0037's commit 79589d4) and `tracks/0160-flight-recorder/research-plan.md` (commit 1bc990e, 2026-08-22). Both carry the dead field corpus category L identified: tracks/0160-flight-recorder/research-plan.md:3 — "**Status**: draft", now one of sixteen identical lines across `tracks/*/research-plan.md`. The corpus's diagnosis of that field: seed-defect-corpus.md:236-241 — "Every `tracks/*/research-plan.md` opens with a `**Status**` line. All fourteen say `draft` — including the eight tracks whose reports are accepted. Nothing updates it, nothing reads it, and `check-docs.ts` does not validate it" — and its explicit deferral, seed-defect-corpus.md:255-257 — "this is exactly the kind of change the report's N=0 finding applies to — the research-plan template has **zero remaining authorings** ahead of it, so changing it propagates into nothing."
- **Why it matters**: 22 of ~26 proposed controls were cut at D-0037, and this arithmetic was one of two findings that "did more work than any argument". It was false within six days and nothing noticed; the same reasoning would decline the repair again today on a premise that has now been falsified twice. This is the acceptance stage letting through a decision whose evidence has an expiry date it does not carry.
- **Proposed fix**: One line appended to D-0037 in place, in the style the ledger already uses for D-0025's wrong number (DECISIONS.md:200): record that "zero remaining authorings" held on 2026-08-17 and has since been falsified by two new plans, and that the category-L repair is therefore back on the table. Do not re-open the whole decision — the correction note is the repair. Also refresh seed-defect-corpus.md:238's counts (fourteen → sixteen, eight → nine).
- **Refuter**: Confirmed, and the timing is worse than the finding claims. DECISIONS.md:285 (D-0037, 2026-08-17) rests on "the research-plan template has **zero remaining authorings**, so changing it propagates into nothing", and seed-defect-corpus.md:255-257 defers the category-L repair on that exact premise. `git log --diff-filter=A` shows tracks/0150-message-inbox/research-plan.md added in 54cc20f at 2026-08-17 23:40 — 56 minutes after D-0037's own commit 79589d4 at 22:44, and `git merge-base --is-ancestor 79589d4 54cc20f` confirms the order — and tracks/0160-flight-recorder/research-plan.md in 1bc990e on 2026-08-23. Both carry `**Status**: draft`; the count is now 16 of 16. The premise was false within the hour and has been falsified twice. The proposed repair is the pattern the ledger already uses in place (DECISIONS.md:200, "**2026-08-15 correction (in place; the original text above is left intact as the record).** 'Ten tracks remain unstarted' is wrong"), adds no stage, no check and no always-loaded bytes. Held at minor: it touches no claim in the artifact under acceptance and reverses no decision — D-0037's ruling stands, only one of its two arithmetic warrants has expired. I would drop the finding's second half (refreshing seed-defect-corpus.md's counts): that file carries "**Gathered**: 2026-08-14" and is a dated snapshot of an accepted track.

#### VAL-1 — The report's headline count of its own under-answered intake items is wrong: it says "Four" where the accepted decision ledger, the README frontier, and the intake file itself all say six — and the report's own assumption list below carries eight.

- **Severity (post-refutation)**: major  ·  **Lane**: machinery:validator
- **Evidence**: tracks/0160-flight-recorder/report.md:796 — "Four of the twelve answers settled less than their question asked, and each is carried below rather than read as closed." Contradicted by: DECISIONS.md:325 (D-0042 **Why**) — "Twelve items came back answered; **six settled less than their question asked**, and four of those were initially read as closed"; README.md:11 — "Six of the twelve answers settled less than their question asked"; intake/2026-08-22-0160-recorder-facts.md:11 — "> **Six answers did not fully close their questions**, and the report carries each as a". The report's own "Still assumed" bullets (report.md:802–812) name eight a–l items as partially answered: REST sizes (a), build-id readability (e), device-id permissibility (i), gzip (c), xstate `version` ("part of item d, unanswered"), header feasibility (h), incumbent handlers ("'none named' rather than 'none exists'", g), and workers ("the only surviving half of item k's answer").
- **Why it matters**: D-0004 makes the declared-assumption ledger the report's compliance surface, and D-0042 — written the same day, citing THIS intake file as its evidence — exists precisely because under-answered items get read as closed. The report understates its own assumption debt by half against a decision that names the correct figure. It also looks like a conflation of D-0042's two numbers (six partial, four of them initially misread). No validator check can ever catch a numeral that disagrees across three files; this is exactly the class check-docs is structurally blind to, and gate-review's own rule ("Re-measure load-bearing numbers rather than accepting them") is the only control.
- **Proposed fix**: Change "Four" to "Six" at report.md:796 and make the sentence's set explicit, or state the counting rule beside the number (e.g. "six by the intake register's reading; eight items are carried as assumptions below because d, g and k also came back partial"). Print the rule beside the number, per the gate-review skill's recorded practice.
- **Refuter**: Confirmed by direct measurement, and it survives every refutation route I tried. report.md:796 reads "Four of the twelve answers settled less than their question asked, and each is carried below rather than read as closed." Three siblings say six: intake/2026-08-22-0160-recorder-facts.md:11, README.md:11, and DECISIONS.md:325 (D-0042, an accepted decision that names both numbers and distinguishes them — "**six settled less than their question asked**, and four of those were initially read as closed"). The report's own enumeration carries more than either figure: report.md:813-824 bullets items a, c, d, e, g, h, i, k, plus b flagged unruled in the fact table at report.md:802 — nine of twelve. The commit that produced the sentence (8de8f9c) says six in its body and then lists four, which is the conflation mechanism. No counting rule yields four. Two corrections to the finding as written: the "Still assumed" bullets are at report.md:813-824, not 802-812 (802-812 is the fact table), and the bullet list is ten items covering eight of the twelve plus open items m and n. Held at major rather than blocker: the enumeration below the sentence is complete and correct, no recommendation or verdict moves, and the repair is one numeral plus the counting rule printed beside it.

#### VAL-2 — The design-FMEA register the report links as its audit trail cites report.md by line number ~199 times, and those line numbers are systematically stale against the revised report — 34 of the 40 mechanically checkable cites land outside the section they name.

- **Severity (post-refutation)**: major  ·  **Lane**: machinery:validator
- **Evidence**: report.md:747 sends the reader there: "its register is [design-fmea-enumeration.md](design-fmea-enumeration.md), and the amendments it forced are annot[ated]...". Measured over the register: 199 `report.md:NNN` cites, max line cited 634 against a 1040-line report. Of the 40 cites of the form `Q<n> ... report.md:NNN`, **34 fall outside that question's actual line range**. Two verbatim examples — design-fmea-enumeration.md:406: "Q11's per-hazard list (report.md:543-547) budgets tap bodies as \"project, meter, push, return\" ... Q13 (555) rules where the rules apply" — but report.md:543-547 is the middle of the `LossMarker` union type, Q11 spans report.md:717-728 ("project, meter, push, return" is at report.md:720) and Q13 spans 733-740. design-fmea-enumeration.md:419: "report.md:415-418 defines `{elided: true; at: string; cause: 'depth' | 'breadth' | 'coercion'}`" — the type is actually at report.md:556-557 and now reads `cause: 'depth' | 'breadth' | 'coercion' | 'circular' | 'unserializable'`, i.e. the register's finding was fixed and the cite still points at the pre-fix location. Same line cites "report.md:221" for "never only in-band strings", which is at report.md:262. checkLinks (scripts/check-docs.ts:127-147) never sees any of these: they are bare prose tokens, not `](...)` links, and `stripInlineCode` blanks the backticked ones outright.
- **Why it matters**: The report under acceptance rests 24 sentences on "the enumeration found X" and offers this register as the way to check them. A reviewer following the cites lands on unrelated text, with nothing in the register's header warning that its line numbers predate the intake revision. The audit trail from report → register → report is broken, and no doc-system invariant can detect it. I ran the one mechanical check that would apply — a `:LINE` cite pointing past the end of its target — over the whole corpus: 259 cites scanned, 212 resolvable, **0 true hits** (3 false positives, all basename collisions on `system.ts`, which is xstate's source not the 0060 spike's adapter). So the checker is a do-not-build under D-0037 and the repair has to be a convention.
- **Proposed fix**: Either add one line to the register's header stating that its `report.md:NNN` cites are against the pre-amendment revision and naming the commit (08d5bbe), or re-anchor the register's cites to headings/`Q<n>` labels rather than line numbers. Prefer the header line — it is one sentence, preserves the register verbatim as claimed, and costs nothing. Do not build a line-cite checker: it scores zero.
- **Refuter**: Confirmed by spot-checking, not by trusting the count. I measured 199 `report.md:NNN` cites in design-fmea-enumeration.md, max line cited 634 against a 1040-line report. Every cite I resolved lands wrong: design-fmea:406 cites report.md:543-547 for Q11's tap-body budget "project, meter, push, return" — report.md:543-547 is the middle of the `LossMarker` union and the quoted phrase is at report.md:721; design-fmea:419 cites report.md:415-418 for `{elided: true; at; cause: 'depth'|'breadth'|'coercion'}`, which now sits at report.md:556-557 reading `cause: 'depth' | 'breadth' | 'coercion' | 'circular' | 'unserializable'` — i.e. the cite points at the pre-fix location of a finding the report has since applied. Same for report.md:411/412, cited for `CapturedRecord.body` and `markers?: readonly LossMarker[]` but actually `configErrors` and `configErrorsOverflow`. The offset is systematic, consistent with the intake revision inserting text. This matters because report.md:747 names the register as the audit trail for the enumeration claims. The finding's own discipline is sound: it measured a line-cite checker at 0 true hits over 259 cites and refuses to build it, proposing instead one header sentence naming the pre-amendment commit. Held at major rather than blocker: the register's `Q<n>`/`I<n>`/mode-id anchors still resolve, so the trail is degraded rather than severed, and the fix is one line in a supporting artifact.

#### VAL-3 — This track roughly doubled the always-loaded README frontier — adding 6,538 characters — without pricing it, which D-0029 requires and which the decision written the same day (D-0042) did comply with.

- **Severity (post-refutation)**: major  ·  **Lane**: machinery:validator
- **Evidence**: CLAUDE.md:8 makes it always-loaded: "Start by reading `README.md` — its track table and one-line **Frontier** pointer say where the". The frontier line (README.md:11) is now 12,510 characters / 12,600 bytes of README.md's 16,634 — **76% of the entry-point file**, ~40 sentences on one line. Git history of that line: 341 chars on 2026-08-14 (8ca332a), 6,485 before this track (bf91589, 2026-08-18), 12,600 today. The 0160 segment alone (from "**0160-flight-recorder is chartered (D-0040)**" to end) is 6,538 chars = 52.3% of the line. DECISIONS.md:227 (D-0029): "a byte added to an always-loaded file is ceiling-free ... because always-loaded bytes are ceiling-free but recur on every session of every track forever, they must still be **priced and reported** in the agent-token unit; ceiling-free is not cost-free, and a report that omits that price is incomplete." D-0042 observes the rule verbatim at DECISIONS.md:324: "the always-loaded price is one bullet in `CLAUDE.md` growing by ~55 words, reported here per D-0029's standing rule." No such pricing exists for the 0160 frontier text in report.md, DECISIONS.md, or the track. For scale, tracks/9900-process-design/report.md:645 cut twelve controls because "this survey's *own* always-loaded proposals would consume 74-122% of the headroom".
- **Why it matters**: The repo installed a strict pricing discipline on always-loaded bytes and killed twelve proposed controls under it — then let the one always-loaded surface that has a validator check grow 37× unpriced. `checkFrontier` (scripts/check-docs.ts:233-237) asserts only `/^>\s*\*\*Frontier:\*\*\s+\S/m` — existence, never a bound — so it is the check that gives the appearance of governing this surface while bounding nothing. The spec calls it "a one-line **frontier pointer** names the next action" (docs/superpowers/specs/2026-08-13-tooling-research-program-design.md:127); it is now a 40-sentence program history.
- **Proposed fix**: Add the D-0029 price to the acceptance entry for 0160 ("frontier grew N chars, paid every session"), and either compress the frontier back to a pointer with the narrative moved into the track, or — if a bound is wanted — extend checkFrontier with a character cap. Priced honestly: a cap scores exactly 1 hit today at any threshold between ~700 and 12,510 chars, it is a rung-0 validator check and therefore ceiling-free under D-0029, and it adds no workflow stage. That is a real hit count, unlike every other check I measured, but the threshold is a ruling for the user, not a checker's to invent.
- **Refuter**: Every number reproduces. README.md line 11 is 12,510 characters / 12,600 bytes against README.md's 16,634 total — 76% of the entry-point file on one line. The 0160 segment from "**0160-flight-recorder is chartered (D-0040)**" to end is 6,586 characters, 53% of the line. Git: the same line was 341 bytes at 8ca332a and 6,485 at bf91589 immediately before this track, so it has grown 37× and has never been compressed. CLAUDE.md:8 makes README always-loaded ("Start by reading `README.md`"). D-0029's rule is on the bytes, not on control proposals: DECISIONS.md:227 "a byte added to an always-loaded file is ceiling-free ... because always-loaded bytes are ceiling-free but recur on every session of every track forever, they must still be **priced and reported** in the agent-token unit; ceiling-free is not cost-free". D-0042 paid it verbatim for a ~55-word CLAUDE.md bullet (DECISIONS.md:324); nothing prices the 0160 frontier text. The asymmetry is stark against tracks/9900-process-design/report.md:645, where twelve controls were cut on always-loaded headroom. checkFrontier (scripts/check-docs.ts:233-237) asserts only `/^>\s*\*\*Frontier:\*\*\s+\S/m`, and the spec calls for "a one-line **frontier pointer**" (docs/superpowers/specs/2026-08-13-tooling-research-program-design.md:127). One caveat worth carrying: D-0029 names no owner for the frontier's price, so the honest finding is an unowned obligation rather than a report that skipped one — and the char-cap threshold is a user ruling, as the finding says. Not a blocker: it indicts no claim in report.md.

#### VAL-4 — The frontier credits a three-lane verification pass with 33 corrections to the report under acceptance, and no artifact in the repo records that pass or any of its findings.

- **Severity (post-refutation)**: minor  ·  **Lane**: machinery:validator
- **Evidence**: README.md:11 — "A three-lane verification pass over the revision (arithmetic, cross-section consistency, fact-vs-assumption honesty) then corrected 33 items in the report's own text, chiefly an unsourced composite byte figure presented as measured, a peak rate used as a mean, and a worker bridge the fact table claimed was reserved when nothing reserved it." Searching the track and the ledger for that pass returns nothing: `grep -rn "33 item|three-lane|verification pass" tracks/0160-flight-recorder/ DECISIONS.md` matches only README.md:11 itself; report.md never mentions it. Its only other trace is a commit subject, 8de8f9c "docs(0160): revise the report against intake a–l, then against its own verification". Contrast the same track's other passes, each with a preserved register: tracks/0160-flight-recorder/plan-fmea-enumeration.md, tracks/0160-flight-recorder/design-fmea-enumeration.md, tracks/0160-flight-recorder/design-panel-record.md — and the design register's own header, "Everything is preserved verbatim in the appendices" (design-fmea-enumeration.md:31).
- **Why it matters**: Three of the 33 findings are named in the frontier and are substantive claims about the report being accepted — a fabricated-looking byte figure, a peak rate used as a mean, an unreserved worker bridge. At the gate, nobody can check whether those were fixed correctly, whether the other 30 were real, or whether any were scoped out. The repo's own machinery lens requires the opposite: ".claude/skills/gate-review/SKILL.md:69-70 — "**Preserve the runners-up verbatim**, with a one-line reason each. Do not discard them." checkFrontier validates that the line exists, never that its claims have referents, so this is invisible to the validator by construction.
- **Proposed fix**: Either land the pass's register alongside the other two (tracks/0160-flight-recorder/verification-pass.md), or drop the "33 items" claim from the frontier and replace it with what is actually recorded — the dated in-place amendment markers in report.md. Do not leave an unevidenced count in the always-loaded file.
- **Refuter**: The gap is real: `grep -rn "verification pass|three-lane|33 item"` over tracks/0160-flight-recorder/, DECISIONS.md and README.md matches only README.md:11 itself, and the track directory holds research-plan, plan-fmea-enumeration, design-fmea-enumeration, design-panel-record and report — no register for this pass. Both other passes have one, and design-fmea-enumeration.md:31 states "Everything is preserved verbatim in the appendices". Downgraded from major because the finding overstates: the commit body of 8de8f9c is a partial register, naming the three substantive corrections the frontier cites plus the six-answers item, the wire arithmetic ("34 / 5 is 6.8, so the compressed range is 3-7 MB") and a bullet covering nine stale-text sites — and this repo mines git history as durable record by its own practice (seed-defect-corpus.md:3-4). What is genuinely unevidenced is the numeral: "corrected 33 items" sits in the always-loaded file with no enumerable referent. The cheaper of the finding's two proposed fixes — drop or substantiate the count — is the right one; landing a full register after the fact is reconstruction, not preservation.

#### VAL-5 — Intake item j's answer is truncated mid-clause, is omitted from the intake header's list of items that came back partial, and the report reads it as a settled and load-bearing fact.

- **Severity (post-refutation)**: minor  ·  **Lane**: machinery:validator
- **Evidence**: intake/2026-08-22-0160-recorder-facts.md:173 is the entire answer and ends mid-sentence: "Two tabs at most today are open for the app. They have distinct views and distinct" (line 174 is blank; line 175 begins "## k"). The file's header note names six partial items and j is not among them — intake:14-27 lists b, a, c, e, i, h only. The report treats it as closed: report.md:809 — "| Two tabs, distinct views (j) | Multi-tab is a live condition: 2× envelope per device, and question 16's liveness answer is load-bearing |", and report.md:751 — "**intake item j now answers N = 2, with distinct views** (2026-08-23), which moves this question from precautionary to load-bearing without changing the design."
- **Why it matters**: D-0042 was ratified the same day on this exact file, and its whole point is the clause-by-clause answer check: "every unanswered clause stays open and is named in the file's header note" (DECISIONS.md:324). A dangling "and distinct ___" is an unread clause about how the two tabs differ — and the candidates that would complete it (distinct sessions, distinct logins, distinct clientIds) bear directly on the single-connection invariant and RD-7 identity, which report.md:751 itself flags as unenforced ("the profile states that as intent, not as an enforced property"). checkIntake (scripts/check-docs.ts:241-249) validates only that a `**Status**:` line exists somewhere in the file; item structure, answer completeness and header-note agreement are entirely unpoliced.
- **Proposed fix**: Add item j to the header note's partial list with the unread clause quoted, and add one bullet to the report's "Still assumed" list: "How the two tabs differ beyond views — item j's answer is truncated. If they carry distinct sessions or clientIds, question 16's identity and single-connection reasoning changes." Do not read the truncated half.
- **Refuter**: Confirmed exactly. intake/2026-08-22-0160-recorder-facts.md:173 is the entire answer to item j and ends mid-clause — "Two tabs at most today are open for the app. They have distinct views and distinct" — with line 174 blank and 175 beginning "## k". The header note's partial list at intake:14-27 names b, a, c, e, i, h only; j is absent, which is a direct miss against D-0042's requirement that "every unanswered clause stays open and is named in the file's header note" (DECISIONS.md:324) — in the very file D-0042 cites as its evidence base. The report reads it as settled at report.md:809 ("Two tabs, distinct views (j)") and report.md:751 ("**intake item j now answers N = 2, with distinct views**"). Held at minor rather than major: the report uses only what the complete half of the sentence says, so no arithmetic or verdict rests on the dangling clause — N=2 and "distinct views" are both fully present — and report.md:751 already flags the adjacent single-connection question as unenforced ("the profile states that as intent, not as an enforced property"). The repair is the header-note line plus one "Still assumed" bullet, and it compounds VAL-1: j would be a further item the count at report.md:796 does not carry.

#### VAL-6 — Item h's answer is filed below the "Items i–l added" divider, so item h reads as unanswered in place while its answer floats orphaned in the i–l preamble.

- **Severity (post-refutation)**: minor  ·  **Lane**: machinery:validator
- **Evidence**: intake/2026-08-22-0160-recorder-facts.md:146 is item h's last line — "→ Resolution: decides plan question 7's crash-complement recommendation" — followed by 147 (blank), then 148-149, the section divider: "*Items i–l added 2026-08-23, from the failure-mode enumeration over the plan" / "([register](<relative link as written in the intake file>)).*", and only then 150: "It's all firefox today, but maybe someday down the line it could be chromium." — which is h's answer, per the header note at intake:25-27 ("**item h** was answered with browser composition (Firefox today, possibly Chromium later)").
- **Why it matters**: A reader scanning item h sees a Resolution line with nothing under it and concludes the item is unanswered; a reader scanning the i–l divider sees a stray sentence with no owner. The report depends on this answer at report.md:802 ("item h's line answered browser composition (Firefox today, Chromium maybe later) rather than the feasibility it asked about") and in the fact table's "**Firefox 124, Firefox only** (f, h)" row. checkIntake sees only the Status line, so a misfiled answer inside an intake file is structurally undetectable.
- **Proposed fix**: Move line 150 up to sit directly under intake:146, before the i–l divider at 148-149. One-line move, no content change; D-0009 (resolved files stay in place) is untouched.
- **Refuter**: Confirmed line by line. intake/2026-08-22-0160-recorder-facts.md:146 is item h's last line ("→ Resolution: decides plan question 7's crash-complement recommendation"), 147 is blank, 148-149 are the section divider "*Items i–l added 2026-08-23, from the failure-mode enumeration over the plan ([register](<relative link as written in the intake file>)).*", and only then 150 carries h's answer, "It's all firefox today, but maybe someday down the line it could be chromium." The header note at intake:25-27 confirms that sentence is h's answer. So item h reads as unanswered in place while its answer floats in the i–l preamble, and the report depends on it at report.md:802 and in the fact table's "**Firefox 124, Firefox only** (f, h)" row. checkIntake (scripts/check-docs.ts:241-249) asserts only the `**Status**:` line, so this is structurally invisible to the validator — correctly stated, and correctly paired with no proposal to build a check. The fix is a one-line move; the file is `**Status**: open`, so D-0009 is untouched. Minor: nothing was lost or misread.

### Completeness critic (8)

#### CRIT-1 — The Recommendation adopts safe-stable-stringify on a mechanism the report's own question 3 falsified in a dated amendment, and calls the bill of materials "verified" while the report's falsification section names the same open question as "the finding that most affects the build's bill of materials."

- **Severity (post-refutation)**: blocker  ·  **Lane**: completeness-critic
- **Evidence**: tracks/0160-flight-recorder/report.md:776 — "Adopt exactly two packages, both D-0027 rung-1 justified in the Survey: safe-stable-stringify 2.5.0 (**cycle-safe bounded projection; wrapped so its in-band elision becomes structured markers**) and tracekit 0.4.9". The report refutes that exact wrapping at report.md:80 — "**The mechanism needs correcting from the obvious one** (added 2026-08-23): a plain `replacer` cannot see breadth elision at all, because the library sorts and *slices* the key list before iterating … **Detecting the library's output strings cannot work either: they are indistinguishable from app strings with the same content.** So the wrapper owns the bound decisions rather than observing them — it walks with its own depth/breadth accounting and hands the serializer already-bounded structures, **or the design accepts an owned walk and re-prices safe-stable-stringify's rung-1 justification down to its cycle handling. The spike settles which**". The residual cycle-handling justification does not survive the same argument: report.md:548-551 requires a structured `cause: 'circular'` marker precisely "because the chosen serializer renders cycles as the in-band string \"[Circular]\"" — an in-band string report.md:80 has just ruled undetectable, so a wrapper that owns the walk owns cycle detection too. Yet report.md:743 states "The build's bill of materials, **verified**: two micro-deps in (safe-stable-stringify, tracekit — both D-0027 rung-1 justified in the Survey)", report.md:794 states "D-0027 (each adopted micro-dep carries its rung-1 justification in the Survey)", and the STE at report.md:7 states it unconditionally: "with **two adopted micro-dependencies** (safe-stable-stringify for the projection core, tracekit for stack parsing)". Against report.md:831 — "**The elision mechanism**: whether bound-hits are detectable at all through safe-stable-stringify's replacer (they are not for breadth, which slices before iterating) and therefore whether the wrapper owns the walk — **the finding that most affects the build's bill of materials**." D-0027 rung 1 (DECISIONS.md:213) permits an npm dependency "for things that do not make sense to roll ourselves" — the justification the report says the spike may re-price to nothing.
- **Why it matters**: The dependency list is the deliverable half of the verdict — it is what the Recommendation line at report.md:14 and the STE both carry, and D-0027 requires a per-dependency justification that this one explicitly does not yet have. As written the Recommendation prescribes a package by describing a mechanism the report proved does not work, and marks as "verified" a bill of materials whose most consequential term the report says is unsettled. A reader implementing this adopts a library whose two advertised bounds (`maximumDepth`/`maximumBreadth`) the design will bypass and whose remaining value (cycle handling) cannot produce the structured marker the record shape requires. This is not conditionality that a careful reader recovers: report.md:776 states a false mechanism, not a hedged one.
- **Proposed fix**: Make the Recommendation say what question 3 found. Replace "Adopt exactly two packages … safe-stable-stringify 2.5.0 (cycle-safe bounded projection; wrapped so its in-band elision becomes structured markers)" with: "Adopt tracekit 0.4.9 outright. safe-stable-stringify 2.5.0 is adopted **conditionally**: its `maximumDepth`/`maximumBreadth` bounds are unusable as the elision-detection mechanism (question 3), so the wrapper owns the walk; whether the residual cycle handling still clears D-0027 rung 1, or the walk should be owned outright, is what the elision-mechanism spike settles." Strike "verified" from report.md:743's bill of materials or qualify it to "verified except for the elision mechanism", and carry the same conditionality into the STE at report.md:7 and the Recommendation line at report.md:14.

#### CRIT-2 — Risk 2's mitigation — the conservative redaction default that stands in for an unruled disclosure policy — cannot be expressed in the report's own deliverable interface, and the report's worked configuration does not implement it.

- **Severity (post-refutation)**: blocker  ·  **Lane**: completeness-critic
- **Evidence**: report.md:787 (Risk 2) — "Until someone with the authority rules on it, **the conservative default holds**; the risk is that a design shipped on 'a scrubber exists' sends field payloads to a cloud endpoint under a policy no one wrote." The default is specified at report.md:739 — "The default until a policy is ruled stays the conservative one — **payloads redact to structural skeletons (keys kept, leaf values redacted) for any bucket carrying app data**, seeded with the incumbent scrubber's key list, and **the config marks which buckets may carry values verbatim**." Neither half exists in the interface. (a) `RedactionRules` (report.md:279-283) is `{ keys?: string[]; paths?: string[]; patterns?: { source; flags?; ruleId }[] }` — three match lists and no redact-all-leaves mode, so "keys kept, leaf values redacted" has no expression. (b) `BucketPolicy` (report.md:246-268) declares only `maxCount`, `maxBytes`, `maxEntryBytes?`, `project?`, `schema` — no verbatim-allowed marker — and question 6 forecloses the alternative at report.md:206-213: "ONE rule set for the WHOLE bundle … **Per-bucket rule sets refused**; path rules provide per-bucket scoping where needed." (c) The worked configuration, the one every byte figure in the report is computed against, configures the opposite of the conservative default — report.md:585-590: "redaction: { keys: ['password', 'token'], paths: ['payload.operator.*'], patterns: [{ source: 'Bearer\\s+[A-Za-z0-9._-]+', flags: 'i', ruleId: 'bearer' }] }" — a two-key denylist that ships every other leaf value verbatim.
- **Why it matters**: This is the risk the STE names as one of the two that matter most, over the one item the report says "should not be closed by inference" (report.md:737). Its entire mitigation is the conservative default, and the default is unbuildable from the interface this track exists to deliver: no config value produces skeleton-redaction, no field grants a bucket verbatim carriage, and the report's own worked table demonstrates the permissive shape instead. So the design as specified ships MQTT and HTTP payload values to a cloud endpoint under a policy nobody has written — exactly the outcome Risk 2 says the default prevents. The repair is a design change to an accepted-track deliverable (a new mode or a new bucket field, against question 6's explicit refusal of per-bucket rule sets), not an editorial one.
- **Proposed fix**: Add the missing operand to `RecorderConfig.redaction` and say which surface carries the exemption. Cheapest consistent version: give `RedactionRules` a `mode: 'denylist' | 'skeleton'` (default `'skeleton'`, leaf values redacted with `{redacted, ruleId: 'default-skeleton'}` markers per RD-10) and add one boolean to `BucketPolicy` — `valuesVerbatim?: boolean` (default false) — noting that this is a per-bucket *policy* bit, not a per-bucket rule set, so question 6's refusal is untouched. Then set the worked example at report.md:585-590 to the conservative default it claims to ship, or state in the comment that the table shows the post-ruling permissive configuration and is not the shipping default.

#### CRIT-3 — The rubric's evidence-asymmetry note names two asymmetric rows, then rests the verdict on Boundedness while claiming its evidence is "symmetric and read from each candidate's own source" — which the build's own Boundedness cell contradicts in the same table.

- **Severity (post-refutation)**: major  ·  **Lane**: completeness-critic
- **Evidence**: report.md:772 — "Two rows carry an **evidence asymmetry** worth naming, because the verdict must not lean on them: Browser compatibility … and Runtime overhead … Both differentials are partly artifacts of what was measured rather than findings about the candidates. **The recommendation rests on capture-safety and Boundedness, where the evidence is symmetric and read from each candidate's own source.**" The build's Boundedness cell says the opposite, at report.md:768 — "strong by design — byte + count caps, a six-counter loss ledger … **the claim is construction plus property tests, and stays provisional until the spike runs them**" — while the three SDK cells in the same row are read from shipped source ("count caps with **counted** overflow (client reports …)", "silent oldest-drop at the crumb cap, misconfiguration falling back to the default 25", "`ItemBuffer` is unbounded, and nothing counts bytes"). The criterion itself is the track's own proposal, adopted at weight high the day before the survey (DECISIONS.md, D-0041: "the track-specific tenth rubric criterion **\"Boundedness\"** is **adopted at weight high**"), and its wording — "does the candidate bound its memory by construction, in bytes as well as items, and does it fail loudly (counters, truncation markers) or silently when a bound is hit" — restates the build posture's design goals, which is why the build scores "strong by design" against libraries scored on behaviour.
- **Why it matters**: The asymmetry note is the paragraph whose job is to stop the verdict leaning on artifacts of what was measured, and it certifies as symmetric the row that carries the largest asymmetry in the table: an unbuilt design's stated intentions against three shipped implementations' verified behaviour, with the build cell's own text conceding the score is provisional. Under D-0001 the build can never have source evidence, so the asymmetry is structural and unavoidable — but the claim of symmetry is not, and it is the sentence a reader uses to decide whether the rubric can carry the verdict. Of the recommendation's two named legs, only capture-safety is genuinely read from each candidate's source.
- **Proposed fix**: Change report.md:772 to name three asymmetries, not two: "Boundedness scores the build on a design and property-test plan that has not run, against three candidates scored on shipped source — a structural asymmetry under D-0001, stated here rather than papered over. **The recommendation therefore rests on capture-safety**, where the evidence is symmetric and read from each candidate's own source, with Boundedness carrying corroboration rather than weight until the spike runs."

#### CRIT-4 — Risk 1's first mitigation — "every piece copies a cited design" — is false for the pieces the risk names as the weakest link, and contradicts the survey finding on which the whole build verdict rests.

- **Severity (post-refutation)**: major  ·  **Lane**: completeness-critic
- **Evidence**: report.md:786 — "1. **The owned capture code is the weakest link, and it runs in other modules' disciplined paths.** Mitigation: **every piece copies a cited design**; the interface isolates taps behind their own throw containment; …". The report's own inventory of copied designs is three, none of them capture-path code — report.md:776: "Copy three designs with citation: Sentry's client-report loss accounting, the loglevel-plugin-remote backoff idiom 0050 already copied, and @statelyai/inspect's serialization recipe" (a health-block counter design, a delivery-loop idiom, and the xstate scrubber). The capture path's own pieces are stated to have no prior art to copy: report.md:20 — "the only non-trivial owned pieces are the byte-budgeted per-bucket ring, the seal/bundle/delivery pipeline, and the vendored OpenAPI payload schema"; report.md:778 — "the hard requirements are owned work under every posture — **no surveyed library bounds memory by bytes, none serializes at capture, none carries per-bucket taxonomy**"; and the seal protocol was not copied but invented and then corrected in-track (report.md:687: "Index-snapshot sealing stays forbidden for the reason the enumeration proved").
- **Why it matters**: Risk 1 is the risk the STE ranks first, and the mitigation clause a reader checks is the one asserting the owned code is not novel. It is novel — the report's central argument for building is precisely that nothing in the field does byte-bounded serialize-at-capture, so there is nothing to copy for the byte meter, the byte-eviction loop, or the seal. Stating the opposite in the honesty section converts the report's strongest survey finding into a false reassurance about the same code, and it is the sentence that would let a reader skip the one real mitigation (the property tests) as merely one of four.
- **Proposed fix**: Replace the first clause with what the survey found: "Mitigation: **the pieces with prior art copy it with citation (loss counters, backoff, the xstate serialization recipe), and the pieces with none — the byte meter, the byte-eviction loop, the seal — are novel by the survey's own finding that no library bounds by bytes or serializes at capture, which is why their properties are fast-check lanes rather than review promises**; the interface isolates taps behind their own throw containment; the spike measures the real operand …"

#### CRIT-5 — The Rationale's closing sentence — the one answering the standing objection to building — sizes the owned surface at "~a few hundred owned lines" on a partial inventory, and claims property-test coverage for the largest itemized chunk, which the report elsewhere says no lane this repo owns can reach.

- **Severity (post-refutation)**: major  ·  **Lane**: completeness-critic
- **Evidence**: report.md:778 — "The build concentrates its risk in **~a few hundred owned lines** whose properties (byte invariants, drop accounting, seal correctness) are **exactly what the accepted fast-check lane property-tests best**." The report's own line estimates already reach ~310 before the pieces it calls hard: report.md:743 "the nets (~200 lines equivalent)", report.md:689 "~30 owned lines" for the pre-arm stub (report.md:780 totals these as "the ~250 lines this report estimates"), and report.md:776 "Own the ring (~60 lines; mnemonist shelved as fallback), **the seal, the bundle assembly, and the schema**" — none of the last three costed, plus the owned redaction applier (report.md:739 "the applier is owned"), the config validator (I3/I8), the delivery loop's four rules (report.md:705-709), the four tap adapters (question 4), and — if the elision spike goes the other way — the entire serialization walk (report.md:80). On coverage: the ~250 lines of nets and stub are exactly what fast-check cannot reach — report.md:747 "the failure nets and the pre-arm stub need a real browser (jsdom cannot arm `window.onerror` faithfully) — **named for 0120's future harness**"; report.md:838 "**Real-browser lane (deferred to 0120's harness …)**: net arming/chaining, the pre-arm stub's queue-and-replay …". 0120 does not exist: README.md:50 — "| 0120-e2e-testing | … | planned |".
- **Why it matters**: This is the sentence that answers a reader who objects to build on owned-code risk, and both of its halves overstate. The line count omits every piece the report itself calls a non-trivial owned piece, and the three named properties (byte invariants, drop accounting, seal correctness) cover the ring and the seal — not the nets, the stub, the delivery loop, the redaction applier, the config validator, or the four adapters. The largest costed chunk is the one with no available lane at all, deferred to a harness in a track that has not started, which makes the coverage claim the reverse of the report's own falsification section.
- **Proposed fix**: Split the claim and cost it honestly: "The build's owned surface is roughly ~250 lines of nets and pre-arm stub, ~60 lines of ring, plus the seal, bundle assembly, redaction applier, config validator, delivery loop and four tap adapters. Its **property-testable core** — the ring's byte invariants, drop accounting and seal correctness — is exactly what the accepted fast-check lane tests best; the nets and the stub are **not reachable** by that lane and wait on 0120's harness, which is why they are named among the unverified claims in question 15."

#### CRIT-6 — The STE breaches D-0007 twice: it runs to four paragraphs against the decision's "max 2", and it names two risks as the ones that matter most while the body twice calls a third the design's largest coverage hole and asks for it to be stated beside the recommendation.

- **Severity (post-refutation)**: major  ·  **Lane**: completeness-critic
- **Evidence**: DECISIONS.md:71 (D-0007) — "Every report.md opens with a prepended \"Summary (STE)\" section following ASD-STE100 core writing rules; **max 2 paragraphs**; content order: what was examined, recommendation + tool, **most important risk**, next step." The 0160 STE has four paragraphs (report.md:5 "This track examined how the application gets a flight recorder…", :7 "We recommend **build**…", :9 "The app facts arrived while this report was being written…", :11 "Two risks matter most…"); every other accepted report's STE is exactly two (measured across 0010, 0050, 0060, 0070). The validator cannot see it — scripts/check-docs.ts:221-227 checks only that the first H2 is "Summary (STE)". On the risk ranking: report.md:11 — "**Two risks matter most.** The owned capture code is the weakest link … And what may leave the device is still unruled" — against report.md:790 (Risk 5) — "**This is the design's largest coverage hole and it has no client-side floor on this fleet — worth stating in the same breath as the recommendation rather than in a footnote**" — and report.md:685 — "This is the largest single hole in the design's coverage, and on this fleet it has no client-side floor."
- **Why it matters**: D-0007 exists so the summary is the fast unambiguous executive read, and it is the only part of a 1,040-line report some readers will read. As written the report ranks its risks one way in the summary and the opposite way twice in the body — and the risk it demotes is the one whose consequence (renderer deaths produce no report at all, with backend work nobody has committed to as the sole compensation) most changes what a decision-maker would ask for before approving the build. The paragraph count is the smaller half, but it is a numeric limit in an accepted decision that the validator is structurally blind to, and 0160 is the only report that exceeds it.
- **Proposed fix**: Compress the STE to two paragraphs per D-0007 — fold the facts paragraph (report.md:9) into the recommendation paragraph, keeping only the two facts that move the design (MQTT depth, Firefox-124 delivery shape). In the risk sentence, either say "three risks matter most" and add the coverage hole, or replace the ranking with the body's own: "The most important risk is that crash-shaped deaths produce no report at all on this fleet, with backend absence-detection nobody has committed to as the only compensation; two more follow — the owned capture code runs inside other modules' latency budgets, and what may leave the device is still unruled."

#### CRIT-7 — The report's one distinct claim is cross-bucket interleaved history, and the window that claim delivers is bounded by the shortest bucket — but the report computes a time span for one bucket only, and never states or declares-unknown the spans of the other three.

- **Severity (post-refutation)**: major  ·  **Lane**: completeness-critic
- **Evidence**: The claim, report.md:59 — "The recorder's distinct claim survives scrutiny, and it is exactly one claim: **retrospective, full-fidelity, cross-bucket context** … 'the last 100 transitions, the last 100 messages, and the last 25 HTTP exchanges as they interleaved'"; reinforced at report.md:525 — "cross-bucket interleaving is the recorder's one distinct claim (Q1)". The report then establishes that a count is not a window, and says so pointedly for MQTT only — report.md:73: "**the depths buy very little history**: at the ~50 msg/s figure the profile records … 100 MQTT messages is about **two seconds** of lead-up … A recorder whose entire claim is 'what preceded the failure' delivering two seconds of it is worth stating plainly" — and acts on it in the worked table at report.md:604: "maxCount: 1_200, // ~24 s at ~50 msg/s; the requested 100 was ~2 s". The other three rows keep the requested counts with no span reasoning at all — report.md:612 "http: { maxCount: 50, maxBytes: 4 * MiB, …", report.md:616 "xstate: { maxCount: 100, maxBytes: 2 * MiB, …", report.md:618 "log: { maxCount: 50, maxBytes: 1 * MiB, …" — the only note being report.md:614-615, "Raise if field bundles show the window is too short." No transition, request, or log-line rate appears anywhere in the report, and none of the three appears in the "Still assumed" list at report.md:813-824 (which declares REST body *sizes* unknown, never rates).
- **Why it matters**: A bundle's cross-bucket history is only as wide as its narrowest bucket: 24 seconds of MQTT interleaves with nothing for the 22 seconds in which the xstate, HTTP and log rings hold no records. The report applies its own "a count is not a window" standard to exactly the bucket where a rate happened to be available, raises that bucket twelvefold on the strength of it, and leaves the claim the raise exists to serve unsized. If xstate transitions fire anywhere near the MQTT rate — plausible, since the report's own comment at report.md:613 notes a transition emits two records — the recommended table delivers a ~2-second cross-bucket window inside a 20 MiB envelope, which is the exact failure the MQTT raise was made to prevent. Under D-0004 three missing rates are a declarable assumption; here they are neither declared nor reasoned about.
- **Proposed fix**: Add one paragraph to question 2 and one bullet to "Still assumed". The paragraph: "The cross-bucket window is the **minimum** of the four bucket spans, not the maximum. Only the MQTT span is computable (item a and the profile's rate); transition, request and log-line rates were not supplied, so the other three spans are unknown and the bundle's interleaved window is unknown with them. Raising MQTT to ~24 s buys cross-bucket context only to the extent the other three reach back as far." The bullet: "**xstate transition, REST request, and log-line rates** — never supplied, so three of the four buckets' time spans are unknown. If any is much faster than assumed, the raised MQTT ring interleaves with an empty window and question 1's distinct claim shrinks to that bucket's span; the fix is a per-bucket span target rather than a count, settled by the spike's field bundles."

#### CRIT-8 — The worked table's MQTT row is annotated "~24 s" without qualification and is described as a worst-case reserve, but its byte cap binds first at anything above typical payload size — at item a's worst case the same row holds ~250 messages, about 5 seconds.

- **Severity (post-refutation)**: minor  ·  **Lane**: completeness-critic
- **Evidence**: report.md:604 — "maxCount: 1_200,                  // ~24 s at ~50 msg/s; the requested 100 was ~2 s" beside report.md:605 "maxBytes: 12 * MiB". Question 2 derives the 24 s figure with the qualifier the comment drops — report.md:73: "**at 10 KB typical payloads** a 12 MiB MQTT bucket holds roughly 1,200 messages, on the order of **24 seconds** at the same rate" — and states the worst case in the same paragraph: "100 messages costs **1 MB typically and 5 MB at the worst-case payload size**", i.e. ~50 KB per message, at which 12 MiB holds ~251 records ≈ 5 s at 50 msg/s. The table's framing note is also wrong for this row specifically — report.md:597-599: "Each maxBytes is a **WORST-CASE RESERVE, not an expected fill**: at the analogy record sizes in Q2, http/xstate/log expect to hold well under a quarter of theirs" — but 1,200 × 10 KB ≈ 12 MB is the *typical* fill of the mqtt row, so its byte cap is the binding constraint at any payload above typical, not a reserve.
- **Why it matters**: The MQTT raise from 100 to 1,200 is the report's one substantive change to the config seeds the user supplied, and it is sold on the 24-second figure. That figure holds only while payloads stay at their typical size; the degradation is steepest exactly when traffic is unusual, which is when a bundle is worth having. A reader tuning the table from the comment alone cannot see that `maxCount` is decorative above ~10 KB average and that the real depth is `maxBytes / mean payload`.
- **Proposed fix**: Qualify the comment and correct the reserve note: "maxCount: 1_200, // ~24 s at ~50 msg/s **at item a's 10 KB typical payload; the 12 MiB byte cap binds first above that — at the ~50 KB worst case this row holds ~250 messages, about 5 s**", and amend report.md:597 to "Each maxBytes is a worst-case reserve for http/xstate/log; **the mqtt row is the exception — its byte cap is sized to the typical fill and binds before maxCount whenever payloads run large**."

### Runners-up — cut by the refuter, preserved verbatim (10)

#### MACH-1 — The intake-revision → three-lane-verification stage lets a headline count contradict its own enumeration and three sibling artifacts, because nothing requires the counting rule to be printed beside the number — and the instance landed in the artifact under acceptance, produced by the very lane ("cross-section consistency", "arithmetic") meant to catch it.

- **Severity (post-refutation)**: blocker  ·  **Lane**: machinery:stages
- **Evidence**: report.md:796 — "Four of the twelve answers settled less than their question asked, and each is carried below rather than read as closed." Contradicted by three siblings that all say six: intake/2026-08-22-0160-recorder-facts.md:11 — "> **Six answers did not fully close their questions**, and the report carries each as a"; README.md:11 — "**Six of the twelve answers settled less than their question asked**"; DECISIONS.md:325 (D-0042) — "Twelve items came back answered; **six settled less than their question asked**, and four of those were initially read as closed". D-0042 therefore *names both numbers and distinguishes them*, and the report attached the four-count to the six-count's sentence. The report also contradicts its own list: report.md:813 "Still assumed, and each with its consequence if wrong:" is followed by bullets covering items a, c, d, e, g, h, i, k (report.md:815-824) plus item b in the fact table (report.md:802, "**The policy is still unruled**") — nine of twelve, not four. The stage that produced the split is visible in its own commit: `git log -1 8de8f9c` — "Six of the twelve answers settled less than their question asked and now carry declared assumptions instead of being read as closed: item e ..., item i ..., item c ..., item h ..." — says six, then enumerates four. This is the identical defect class this repo already classified as a gate blocker: .claude/skills/gate-review/SKILL.md:87-89 — "Re-measure load-bearing numbers rather than accepting them. ... the defect was an unstated counting rule, not a wrong figure. Print the rule beside the number."
- **Why it matters**: The number is the reader's summary of how much of the report's factual base is assumed rather than supplied. A reader accepting on "four" is accepting a report whose own list carries nine. The lesson that would have prevented it is written down in the gate-review skill but is executed by no stage — it is a convention stated in a place nothing enforces, which is exactly what this lens is asked to find.
- **Proposed fix**: In report.md:796 replace "Four" with the number the report's own list carries, and print the rule beside it (e.g. "Nine of the twelve answers left at least one clause unanswered; four of those were initially read as closed"). Better and cheaper still, apply gate-review Lens 1 check 1 (SKILL.md:45-46, "exactly one owns it and the others cite it rather than restating it. Restatement is where siblings drift"): make the intake file's header note the sole owner of the count and have the report and README cite it rather than restate it. No validator — D-0037's own rule is to count hits before writing a check, and there is one hit.
- **Cut because**: The instance is real and I reproduced it exactly — report.md:796 "Four of the twelve answers settled less than their question asked" against intake/2026-08-22-0160-recorder-facts.md:11 "**Six answers did not fully close their questions**", README.md:11 "**Six of the twelve answers settled less than their question asked**", DECISIONS.md:325 "**six settled less than their question asked**, and four of those were initially read as closed", and the commit body of 8de8f9c which itself says "Six of the twelve answers settled less than their question asked" and then enumerates only e, i, c, h. The report's own list at report.md:813-824 covers items a, c, d, e, g, h, i, k plus b at report.md:802, so no counting rule yields four. But this is the SAME instance VAL-1 reports, and it should be counted once — I sustain it there. The machinery framing is refuted on its own terms: the fix it proposes is the convention it quotes as already written (.claude/skills/gate-review/SKILL.md:87-89 "Print the rule beside the number" and SKILL.md:45-46 Lens 1 check 1 "exactly one owns it and the others cite it rather than restating it"), so it restates a repo convention rather than identifying a missing stage, and it explicitly declines to add any control. Blocker severity is also over-set: the report's enumeration immediately below the sentence is complete and correct, no verdict or recommendation moves, and the repair is one word.

#### MACH-2 — The intake answering stage has no completeness check on the answer text itself, so an answer that is literally cut off mid-sentence is recorded, passes D-0042's clause-by-clause re-read, and becomes a load-bearing fact in the report.

- **Severity (post-refutation)**: major  ·  **Lane**: machinery:stages
- **Evidence**: intake/2026-08-22-0160-recorder-facts.md:173 is the entire answer to item j and ends mid-clause: "Two tabs at most today are open for the app. They have distinct views and distinct" (line 174 is blank; line 175 begins "## k"). The item asked (line 166-170) "How many tabs of the app are typically open on a device — and is more than one ever legitimate, even transiently? ... connects to the single-connection enforcement question intake 2026-08-17-0150 item f already raised." The report reads it as settled: report.md:809 — "| Two tabs, distinct views (j) | Multi-tab is a live condition: 2× envelope per device, and question 16's liveness answer is load-bearing |", and report.md:751 — "**intake item j now answers N = 2, with distinct views** (2026-08-23), which moves this question from precautionary to load-bearing". The truncated clause is plausibly about exactly the open question the same paragraph then has to hedge: report.md:751 — "with two tabs, and one MQTT connection if the single-connection invariant holds — the profile states that as intent, not as an enforced property, with enforcement still open as 0150's intake item f". Item j is not among the six the header note lists as partial (intake:14-27).
- **Why it matters**: N=2 is load-bearing arithmetic, not colour: report.md:791 computes "~40 MiB of serialized bytes per device" from it, and report.md:73 defends charter compliance with "Even the assumed total sits inside the charter's 10 MB floor for one tab". D-0042 exists because "an under-answered item reads as closed" (intake/_TEMPLATE.md:18-19); a sentence that stops mid-word is the cheapest possible instance of that and it still got through, in the very file D-0042 cites as its evidence base (DECISIONS.md:326).
- **Proposed fix**: Add one line to the answer-intake step in intake/_TEMPLATE.md's HOW-TO block (currently steps 1–4 at lines 49-63): before re-reading clause by clause, confirm each recorded answer is a complete sentence — a truncated capture is a missing answer, not a partial one. Then reopen item j for its last clause and, until it lands, move it into the header note's partial list and into report.md:813's "Still assumed" bullets with its consequence (a third tab, or two connections, changes both the envelope arithmetic and question 16's diagnosis story).
- **Cut because**: The instance is real — intake/2026-08-22-0160-recorder-facts.md:173 is the whole answer to item j and ends mid-clause ("Two tabs at most today are open for the app. They have distinct views and distinct", line 174 blank, line 175 "## k"), and j is absent from the header note's partial list at intake:14-27 (b, a, c, e, i, h). But VAL-5 reports the same instance and I sustain it there. The machinery claim — that the answer-intake stage lacks a completeness check — is refuted because D-0042's step 1 already requires it: intake/_TEMPLATE.md:49-51 "Re-read each item's question CLAUSE BY CLAUSE against the answer. Every clause that went unanswered stays open and is named." An answer that stops mid-word fails that check as written; the proposed extra template line restates step 1. The load-bearing arithmetic is also unaffected: the complete half of the sentence ("Two tabs at most today are open") is what supplies N=2, so report.md:791's ~40 MiB and report.md:73's floor comparison do not rest on the truncated clause.

#### MACH-3 — The D-0004 declared-assumption stage has no shape convention and the report template scaffolds none, so every track invents its own — and 0160 invented one with no assumption identifiers, removing the only anchor this repo has ever used to correct a falsified assumption in place.

- **Severity (post-refutation)**: major  ·  **Lane**: machinery:stages
- **Evidence**: templates/report.md:32-33 is the whole of the guidance: "Rationale and risks. Cite the D-#### constraints applied and the facts/app-profile.md fields used; state each assumption made where a fact was missing." No heading, no ID scheme. Four accepted reports each chose differently: tracks/0060-transport-abstraction/report.md:166 — "- **A-5**: QoS 0/1 with `clean: true` sessions"; tracks/0070-state-concurrency/report.md:164 — "**Ordering metadata is assumed absent** (A-1, the pivotal unknown)"; tracks/9900-process-design/report.md:1001 — "### Assumptions declared (D-0004 discipline...)" with A-1..A-6; tracks/0050-logging/report.md:211 — "**Assumptions in place of facts**". tracks/0160-flight-recorder/report.md contains zero `A-#` identifiers (grep returns nothing); its assumptions are unnumbered prose bullets under report.md:813 "Still assumed, and each with its consequence if wrong:". The IDs are load-bearing precisely when an assumption dies: tracks/0060-transport-abstraction/report.md:167 — "> **2026-08-17 correction (in place; the original text above is left intact as the record). A-5 is FALSIFIED.**", and DECISIONS.md and README.md:11 both address that event by ID — "falsifies 0060 A-5 and 0070 A-6 (both corrected in place)". D-0029 does the same to a report assumption: DECISIONS.md:228 — "**This supersedes assumption A-1 of the 9900 report**, which is annotated in place there."
- **Why it matters**: Two of 0160's assumptions are already known-pending (intake items m and n, report.md:820-821), so falsification is scheduled, not hypothetical. The repo's only working repair mechanism — a dated in-place correction attached to a citable ID — is unavailable for this report, and a future ledger entry that supersedes one of these bullets will have nothing to name.
- **Proposed fix**: Number the bullets at report.md:815-824 A-1..A-10 in place (one edit, no new section), and add one line to templates/report.md's Recommendation block: "List assumptions as **A-#** bullets so a later falsification can be annotated against an ID, the way 0060 A-5 and 9900 A-1 were." Do not build a validator check for it — per D-0037 the corpus count is one report deep.
- **Cut because**: The measurement holds — `grep -c "A-[0-9]" tracks/0160-flight-recorder/report.md` returns 0, and templates/report.md:32-33 is the whole of the guidance ("state each assumption made where a fact was missing"), which prescribes no ID scheme. But the finding's premise that 0160 "invented" a shape is false: tracks/0050-logging/report.md:211 "**Assumptions in place of facts** (facts/app-profile.md is unfilled):" is followed by unnumbered prose bullets with no A-# anywhere in the file — an accepted report with exactly the shape 0160 used. So 0160 conforms to an existing accepted precedent and violates no convention, template rule, or decision. What remains is a preference for a convention this repo has never written down, proposed on a corpus where the practice is already split 3-to-1 rather than uniform. Not a defect in the artifact under acceptance.

#### MACH-4 — The charter stage lets a binding number through without its operand, and no later stage requires the unit to be supplied before it is relied on — so the artifact under acceptance chose the unit for the charter that constrains it, and a ledger entry then ratified the report's own reading.

- **Severity (post-refutation)**: major  ·  **Lane**: machinery:stages
- **Evidence**: DECISIONS.md:309 (D-0040) states the ruling with no scope unit: "**(3) The capture envelope is 10–50 MB.**" The report supplies the unit itself: report.md:73 — "Even the assumed total sits inside the charter's 10 MB floor for one tab; two tabs at worst-case payloads is the first figure that would press it, which is one reason the worked configuration denominates its envelope per tab and says so", and report.md:751 — "the envelope is **per tab**, the device pays 2× — 40 MiB of serialized bytes at the worked configuration". DECISIONS.md:331 (D-0043) then records the report's reading as the charter's: "its envelope is a charter ruling (D-0040, 10–50 MB), worked at 20 MiB per tab". The go gate (D-0041, DECISIONS.md:315-321) reviewed the rubric criterion and the survey posture and says nothing about the envelope's unit. This repo has already paid for this exact class once: D-0029 exists only to supply a unit a ceiling was ruled without — DECISIONS.md:228, "The 9900 report assumed a step was 'a mandatory per-track authoring or review action' (its assumption A-1) and flagged that its arithmetic was contingent on the definition" — and the ledger's own reading order advertises the pair, DECISIONS.md:14: "D-0029 (the ceiling's unit) · D-0035 (the ceiling's number)".
- **Why it matters**: The unit is not cosmetic here. Read per-device, the worked configuration consumes 40 of the charter's 50 MB at N=2 and breaches it at N=3 — and N comes from intake item j, whose answer is the truncated sentence in MACH-2. A charter number whose compliance depends on a fact the intake did not finish supplying is a compliance claim nobody can check, and the ledger has now recorded it as settled.
- **Proposed fix**: One sentence in report.md near line 73 stating plainly that D-0040's figure was ruled without a scope unit, that this report reads it as per-tab, and what the per-device total is at the observed N — i.e. declare the reading as a ruling the report made, per gate-review Lens 1 check 6 ("propose, never assert"). Structurally: add to the go-gate stage the single question "does every charter number carry its operand?", which is where D-0029's lesson belongs and currently is not.
- **Cut because**: DECISIONS.md:309 does state "**(3) The capture envelope is 10–50 MB.**" with no scope unit — that part checks out. The rest does not. The report does not silently choose the unit: report.md:73 says "one reason the worked configuration denominates its envelope per tab and says so", and report.md:751 states "the envelope is **per tab**, the device pays 2× — 40 MiB of serialized bytes at the worked configuration" — the reading and its per-device consequence are both on the page. The user then ratified that reading and performed the per-device arithmetic themselves in a ruling: DECISIONS.md:331 "its envelope is a charter ruling (D-0040, 10–50 MB), worked at 20 MiB per tab" and "0160 contributes ~40 MiB of serialized bytes per device ... which is under 2%". The dependency on MACH-2 also fails: N=2 comes from the complete half of item j's sentence. And the structural repair — "add to the go-gate stage the single question 'does every charter number carry its operand?'" — adds a mandatory gate item with no retirement named, which D-0029's net-steps-hold and D-0035 require.

#### MACH-5 — "One-off" is an unbudgeted stage class: D-0035 caps stages per workflow and explicitly refuses to sum across the workflows a track uses, and D-0030's payment rules have no mirror rule saying that a repeated one-off has become a standing control — so a track can add unbounded review passes, each self-certifying as within the ceiling.

- **Severity (post-refutation)**: major  ·  **Lane**: machinery:stages
- **Evidence**: 0160 ran at least four passes beyond the base lifecycle, each declaring itself exempt. plan-fmea-enumeration.md:9-10 — "This is a one-off pass on one plan, not a standing per-plan control; making it standing would be a D-0029/D-0035 ceiling decision needing its own ruling", and line 12 — "**Shape**: one workflow stage (within the D-0035 ceiling), eight parallel hunters". design-fmea-enumeration.md:13-14 repeats the claim — "It is a one-off pass over one artifact, not a standing control — the same shape and the same limits as the plan-level pass" — while its own line 3 says the opposite of the same pass: "The **design-level** pass the research plan made a standing obligation (plan question 15...)". Its line 16 adds "Eleven agents, two stages". Add the six-lane grounding sweep (DECISIONS.md:309, "A six-lane **plan-grounding sweep** ran at charter time"), the three-lane verification pass (README.md:11), and this gate review. The escape is written into the rule: DECISIONS.md:269 (D-0035) — "One number, applied per workflow — not summed across the several workflows a track uses, and not split into per-class budgets." And D-0036 already banks a third one-off: DECISIONS.md:278 — "**Deferred, not dropped**: the **one-off lifecycle FMEA**".
- **Why it matters**: D-0035's warrant is a stage census that ends at six ("9900's own survey at 6 ... Seven therefore sits **one above the highest run in the repo's history**", DECISIONS.md:270). That census counted only survey workflows; 0160's total pass count is now well above it and the ceiling cannot see it, because each pass is a separate workflow or a one-off. The growth trend the ceiling was minted to catch ("the survey shape went 3, then 5, then 6 across three waves") is now occurring through a route the rule explicitly declines to measure.
- **Proposed fix**: Do not add a budget mechanism — that would itself be a stage. The smallest honest repair is one line appended to D-0035's record at the next gate: re-run its stage census over 0160, record the per-track total, and state explicitly either that per-track totals are unbudgeted by design or that a second one-off of the same shape converts it into a standing control needing an offset. Also fix design-fmea-enumeration.md's internal contradiction (line 3 "standing obligation" vs line 13-14 "not a standing control") — the plan made it standing, so the ledger, not the register, should say which it is.
- **Cut because**: The refusal this finding asks for is already on the record, in the exact words it quotes as the escape hatch: DECISIONS.md:269 (D-0035) "One number, applied per workflow — not summed across the several workflows a track uses, and not split into per-class budgets", ruled on a measured census (DECISIONS.md:270, every survey workflow the repo has run, 3/5/3/6, ceiling set at 7). The proposed fix — append a line to D-0035 stating "per-track totals are unbudgeted by design" — is a verbatim restatement of the sentence already there. The claimed internal contradiction in design-fmea-enumeration.md also dissolves on reading: line 3 says the pass was made "a standing obligation" by *this track's* plan question 15 (a one-time obligation on this report, discharged and stated so at report.md:794 "The plan's question 15 obligation is discharged"), while lines 13-14 say it is "not a standing control" in the repo-wide D-0029/D-0035 sense. Both FMEA registers say the same thing (plan-fmea-enumeration.md:9-10). No new evidence is offered against a decision refused on measured grounds.

#### MACH-6 — A review pass that changed 33 items in the artifact under acceptance left no durable register; its only surviving record is the README frontier paragraph, which the repo rewrites every cycle by design. Nothing in the process requires a pass to preserve what it found and did not fix.

- **Severity (post-refutation)**: major  ·  **Lane**: machinery:stages
- **Evidence**: README.md:11 is the sole standing record: "A three-lane verification pass over the revision (arithmetic, cross-section consistency, fact-vs-assumption honesty) then corrected 33 items in the report's own text, chiefly an unsourced composite byte figure presented as measured, a peak rate used as a mean, and a worker bridge the fact table claimed was reserved when nothing reserved it." `tracks/0160-flight-recorder/` holds five files (research-plan, plan-fmea-enumeration, design-fmea-enumeration, design-panel-record, report) and none is this pass's register; grep for "verification pass" over report.md returns nothing. The only other copy is the commit body of 8de8f9c, which enumerates six of the 33. The frontier is explicitly a moving pointer, not an archive: CLAUDE.md:8-9 — "its track table and one-line **Frontier** pointer say where the program stands and what is next". The requirement the pass skipped is D-0036's: .claude/skills/gate-review/SKILL.md:69-70 — "**Preserve the runners-up verbatim**, with a one-line reason each. Do not discard them. This is also the repair for seed-corpus category J, where findings scoped out of a decision were lost." Both FMEA passes complied (each has a register); this one was not asked to.
- **Why it matters**: 27 of the 33 corrections are recorded nowhere, and anything the pass raised and chose *not* to fix is gone — which is precisely corpus category J ("a scoped-out finding that is not written down is just a forgotten one", seed-defect-corpus.md:170-171). The next frontier rewrite deletes even the summary. A gate cannot audit corrections it cannot see.
- **Proposed fix**: Write the pass's findings to `tracks/0160-flight-recorder/verification-pass.md` from the transcript/commit while it is still recoverable, in the same shape the two FMEA registers already use (shortlist + preserved remainder with one-line reasons). Structurally: extend D-0036's preserve-the-runners-up requirement, which currently binds only the machinery lens, to any pass that amends an artifact under acceptance — a one-line ledger note, not a new stage.
- **Cut because**: The gap is real and I reproduced it — `grep -rn "verification pass|three-lane|33 item" tracks/0160-flight-recorder/ DECISIONS.md README.md` matches only README.md:11, and the track directory holds five files, none of them this pass's register. But VAL-4 reports the same instance with a cheaper fix and I sustain it there. Two things refute this version. "Its only surviving record is the README frontier paragraph" is false: the commit body of 8de8f9c carries a partial register (the three substantive corrections named in the frontier, plus the six-answers item, the wire arithmetic, and a bullet covering nine stale-text sites), and this repo explicitly treats git history as durable evidence — seed-defect-corpus.md:3-4 "**Gathered**: 2026-08-14, by mining this repo's own record — the full git history". And the structural fix — "extend D-0036's preserve-the-runners-up requirement ... to any pass that amends an artifact under acceptance" — converts a gate-lens rule into a standing obligation on every amending pass, repo-wide, with no retirement named.

#### MACH-8 — The "living defect register" is not living: nothing updates it when a recorded defect recurs, and CLAUDE.md points every new session at it as though it were current.

- **Severity (post-refutation)**: minor  ·  **Lane**: machinery:stages
- **Evidence**: CLAUDE.md:50-51 — "Track 9900-process-design is accepted (D-0037). Its living defect register is `tracks/9900-process-design/seed-defect-corpus.md`." `git log -- tracks/9900-process-design/seed-defect-corpus.md` ends at 10b8b78 (2026-08-17), before 0150 and 0160 existed. Its counts are now stale — seed-defect-corpus.md:238 "All fourteen say `draft` — including the eight tracks whose reports are accepted" against sixteen plans and nine accepted tracks today — and two fresh instances of category L (0150, 0160) plus at least one of category K (this track's intake answered in two passes, a–l then m–n) went unrecorded.
- **Why it matters**: Preserved as a runner-up: low consequence today, but the register is the repo's only defect memory, and a register that silently stops accruing instances degrades into a historical document while still being advertised as current — which is corpus category L's own shape ("affordance and practice disagreeing") applied to the register itself.
- **Proposed fix**: Add a dated "last surveyed" line to the corpus header so a reader can see the counts are a snapshot, and append the two new category-L instances in one line each. Do not build an updater or a validator check — the honest fix is a date, not a mechanism.
- **Cut because**: The proposed repair already exists in the file. seed-defect-corpus.md:3-4 opens "**Gathered**: 2026-08-14, by mining this repo's own record — the full git history, all 25 `DECISIONS.md` entries..." — a dated snapshot line, plus a second self-dating signal in "all 25 DECISIONS.md entries" against today's 43. The finding asks for "a dated 'last surveyed' line to the corpus header so a reader can see the counts are a snapshot"; a reader already can. The git-log claim is accurate (last touched 10b8b78, 2026-08-17) and the counts at seed-defect-corpus.md:238 are stale against 16 plans, but the repo's own convention treats a dated artifact as a snapshot rather than a live document (DECISIONS.md, D-0034 Affects: "a report is a dated snapshot and carries an 'As of' line — the convention applies going forward rather than retroactively"). Nothing here bears on 0160's acceptance.

#### MACH-9 — D-0033's bound on the gate-review skill governs its content but not its cadence, so the one claim the skill makes about cadence — that it is batched and does not run per track — drifted with nothing to catch it, on the first gate after it was written.

- **Severity (post-refutation)**: minor  ·  **Lane**: machinery:stages
- **Evidence**: .claude/skills/gate-review/SKILL.md:13-14 — "**This adds no per-track step.** It runs at **acceptance gates**, which are batched — four gate events have covered nine acceptances. It does not run once per track. If deleting this file would change what anyone does per track, the file has overstepped and should be cut back." This gate covers exactly one track (0160). The bound that was meant to keep the file honest is scoped to content only: SKILL.md:19-20 — "It may not add a single item, trigger, or lens that the observed runs did not have."
- **Why it matters**: Preserved as a runner-up because the drift is benign in effect — more review, on the user's directive — but it makes a written self-limitation false on its first exercise, and a document whose factual claims about its own use go stale is the failure D-0032 relabelled the ledger's opening block to prevent ("an enumeration presented as complete was partial from birth", DECISIONS.md:249).
- **Proposed fix**: Do not build a batching rule — forbidding a single-track gate would refuse a review the user asked for. Change SKILL.md:13-14 from a claim about frequency to a description of the record: "It has run at acceptance gates, batched — four gate events over nine acceptances — and once over a single track (0160)." That keeps the file a record of practice, which is what D-0033 shipped it as.
- **Cut because**: The quote is accurate (.claude/skills/gate-review/SKILL.md:13-14, "It runs at **acceptance gates**, which are batched — four gate events have covered nine acceptances. It does not run once per track") but nothing in it has become false. The operative self-limitation is the sentence before it, "**This adds no per-track step**", and its own test, "If deleting this file would change what anyone does per track, the file has overstepped" — deleting it would not; no track workflow gained a stage. The historical sentence ("four gate events have covered nine acceptances") remains true of the record it describes; a fifth gate event that happens to carry one acceptance in its batch does not convert the pass into a per-track obligation, which is what the sentence forbids. The finding also concedes the effect is benign and preserves itself as a runner-up. Editing SKILL.md to record it costs bytes in a D-0033-bounded file for no behavioural change.

#### MACH-10 — Intake file mechanics are unvalidated and the answering convention is unfollowed: an answer can be recorded outside the item it answers, and none of the twelve 0160 answers uses the format the template prescribes, because `check-docs.ts` inspects only the `**Status**` line.

- **Severity (post-refutation)**: minor  ·  **Lane**: machinery:stages
- **Evidence**: intake/2026-08-22-0160-recorder-facts.md:146-150 — item h's "→ Resolution: decides plan question 7's crash-complement recommendation" is followed by the section divider "*Items i–l added 2026-08-23, from the failure-mode enumeration over the plan ([register](<relative link as written in the intake file>)).*" and only then by h's answer, "It's all firefox today, but maybe someday down the line it could be chromium." — so the answer sits below the divider, outside its item. None of the twelve answers uses the prescribed form: intake/_TEMPLATE.md:71 — "**Answering**: append a paragraph under an item, starting `YYYY-MM-DD update — `." The validator's whole intake rule is scripts/check-docs.ts:241-249, whose only assertion is `/^\*\*Status\*\*:\s*(open|resolved)\s*$/m`.
- **Why it matters**: Preserved as a runner-up: no answer was lost here, and D-0042 exempts existing files from rewriting. But the misplaced answer is the mechanical form of corpus category K ("the append-only answer convention goes stale when an item is answered in two passes"), and the unused date prefix is the affordance-vs-practice gap of category H recurring inside the very template minted to close it — the next intake round is where it costs something.
- **Proposed fix**: Move item h's answer under item h in place (the file is not resolved, so this is not a rewrite of a closed record). For the convention: nothing to build — the fix is that the next intake round actually uses the `YYYY-MM-DD update — ` prefix the template already prescribes, and that its header note names any item whose answer was appended out of position.
- **Cut because**: The misfiled answer is real — intake:146 is item h's Resolution line, 148-149 the "*Items i–l added 2026-08-23*" divider, and 150 "It's all firefox today, but maybe someday down the line it could be chromium." is h's answer, sitting below the divider — but VAL-6 reports that instance and I sustain it there. The second half is refuted as a restatement: the `YYYY-MM-DD update — ` prefix at intake/_TEMPLATE.md:71 has been in the template since 3786325 (2026-08-14), and the finding's own proposed fix is "nothing to build — the fix is that the next intake round actually uses the [prefix] the template already prescribes." That is the convention, restated. The validator half is also a non-finding by the finding's own reasoning: it names no check to build and correctly notes D-0042 exempts existing files from rewriting.

#### VAL-7 — Do-not-build register: five candidate validator checks measured against the corpus per D-0037, all scoring zero — reported because a zero-scoring check is a result, and because the one check-docs comment that carries a measurement still reproduces exactly.

- **Severity (post-refutation)**: minor  ·  **Lane**: machinery:validator
- **Evidence**: (a) Dangling `D-####` — re-measured today: 43 declared ids in DECISIONS.md, **853 references** across the corpus, **0 dangling**. The refusal recorded at .claude/skills/gate-review/SKILL.md:90-91 ("451 references, 27 IDs, zero dangling") holds at 1.9× the reference count. (b) `file.md:NNN` cite pointing past the end of its target: 259 cites scanned, 212 resolvable, **0 true hits** (3 false positives, all `system.ts` basename collisions between xstate's source and tracks/0060-transport-abstraction/spikes/boundary-wiring/src/adapters/system.ts). (c) Extending checkLinks to `.claude/skills/` minus the vendored D-0017 tree: 2 files (design-panel/SKILL.md, gate-review/SKILL.md), **0 hits**. (d) A README row with status `accepted` and no D-entry naming that track dir: **0 of 9**. (e) A track directory containing report.md while its row says planned/surveying: **0 of 16** (the six `planned` rows — 0100, 0110, 0120, 0130, 0140, 0150 — hold research-plan.md only, plus 0150's spikes/ under the D-0039 exception the README already documents). Separately verified: the deliberate-exclusion comment at scripts/check-docs.ts:288-294 ("Measured 2026-08-17: including templates/ adds exactly 3 errors, all in templates/spike/") still reproduces exactly — 3 errors, the same 3 paths it names.
- **Why it matters**: D-0037's rule is to count hits before writing a check, and the gate is where that discipline gets paid. Four of the five would be plausible-sounding additions to check-docs.ts; all four would ship as permanent no-ops that only add reading cost to an always-loaded rule set. Recording the zeroes is what stops them being re-proposed at the next gate, which is the same reason the dangling-D refusal was written down in the first place. Note also what the zeroes imply about VAL-2: this repo's real citation defect is semantic drift inside a valid range, which no regex reaches.
- **Proposed fix**: Build none of (a)-(e). If any of this is worth keeping, it is one line in the gate-review skill's closing list recording the re-measurement ("853 references, 43 IDs, zero dangling — re-measured 2026-08-23"), which is a record of practice rather than a new check and therefore inside D-0033's bound. The only check I measured with a non-zero hit count is the frontier bound in VAL-3.
- **Cut because**: A null register indicts nothing in the artifact under acceptance and quotes no text from it, so it is not a finding under this gate's own definition. I re-ran the headline measurement and it does not reproduce as stated: a plain `grep -rohE '\bD-0[0-9]{3}\b' --include='*.md' --include='*.ts'` returns **890** references, not 853, against 43 declared IDs — zero dangling either way, so the conclusion holds but the figure is another instance of the unstated-counting-rule defect the same document cites at SKILL.md:87-89. I confirmed the two claims that were cheap to check: 43 `### D-0` headings in DECISIONS.md, zero dangling references, and the deliberate-exclusion comment at scripts/check-docs.ts:285-296 does still name exactly the 3 templates/spike/ links it claims. The right disposition is what the finding itself recommends — build none of (a)-(e) — which means there is nothing here for a gate to act on, and recording it in SKILL.md would add bytes to a D-0033-bounded file that already carries the refusal at SKILL.md:94-95 ("451 references, 27 IDs, zero dangling").
