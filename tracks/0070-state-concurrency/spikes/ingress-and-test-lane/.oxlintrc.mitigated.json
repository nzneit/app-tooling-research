{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "categories": {},
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["**/transport/*"],
            "message": "single-writer (design.md invariant 2 / D-0002): only the composition root may import the transport feed."
          }
        ]
      }
    ]
  },
  "overrides": [
    {
      "files": ["**/stores/**"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "paths": [
              {
                "name": "@tanstack/react-query",
                "message": "no-store-copy (design.md invariant 5): a store module must not read the QueryCache."
              }
            ],
            "patterns": [
              {
                "group": ["**/query/*"],
                "message": "no-store-copy (design.md invariant 5): a store module must not import the query layer."
              },
              {
                "group": ["**/transport/*"],
                "message": "single-writer (design.md invariant 2 / D-0002): only the composition root may import the transport feed. RESTATED here because oxlint's overrides REPLACE the base rule options rather than merging them — see findings.md."
              }
            ]
          }
        ]
      }
    },
    {
      "files": ["**/composition-root.ts"],
      "rules": {
        "no-restricted-imports": "off"
      }
    }
  ]
}
